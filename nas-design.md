# 사내 파일 서버 (가칭) — 설계 문서

> 상태: 초안 / 작성일 2026-08-08 / 개정 2026-08-07 (자격증명·계정관리 결정 반영)
>
> **이번 개정에서 확정된 것**
> - 자격증명은 **tdbsam 하나**. 웹은 `ntlm_auth`로 같은 것을 검증한다 → [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)
> - 계정 생명주기는 **usersync**에 위임, uid/gid 대역 `3000–19999` → [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)
> - 초기 배치는 기존 서버(karina) 위, 이후 전용 장비로 이전
> - 온프렘 AD 도입 예정이나 **시기 미정** → [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)

---

## 1. 목적과 배경

사내 개인·팀 단위 파일 저장소를 구축한다. 상용 NAS 어플라이언스와 기존 오픈소스는 다음 이유로 부적합하다고 판단했다.

| 후보 | 부적합 사유 |
|---|---|
| Nextcloud | 무겁고, Unix 사용자와 매핑되지 않음. 자체 권한 모델이 파일시스템과 별개 |
| Seafile | 파일을 블록으로 쪼개 저장. POSIX 뷰가 없어 SMB·기존 도구와 병행 불가 |
| Filebrowser | 다중 사용자 권한 모델이 빈약 |
| 상용 NAS | 요구에 맞추기 어렵고 확장 경로가 벤더에 종속 |

따라서 **Go로 웹 UI + 관리 계층만 직접 구현**하고, 파일 접근 프로토콜과 권한 모델은 검증된 기존 구성요소(Samba, POSIX)에 위임한다.

### 설계 원칙

1. **진실의 원천은 파일시스템 하나.** 권한은 POSIX uid/gid/mode/ACL로만 표현한다. 애플리케이션 레벨 권한 모델을 별도로 두지 않는다.
2. **바퀴를 재발명하지 않는다.** SMB는 Samba가, 임퍼소네이션은 커널이, 원자성은 `rename(2)`가 담당한다.
3. **동시성보다 복구 가능성.** 락으로 충돌을 막는 대신, 덮어쓴 데이터를 항상 되살릴 수 있게 한다.

---

## 2. 요구사항

### 기능

- 웹 UI로 파일 브라우징 / 업로드 / 다운로드 / 공유 — **필수 경로.** 컴퓨터에 익숙하지 않은 사용자가 SMB 마운트 없이 사용
- SMB 마운트 — 익숙한 사용자를 위한 부가 경로. 개인 디렉터리 + 소속 팀 공유 디렉터리
- 개인 디렉터리(비공개) 및 팀 공유 디렉터리(그룹 단위 읽기/쓰기)
- 두 경로가 **동일한 데이터, 동일한 권한 규칙** 위에서 동작

### 비기능

- 초기: 스토리지 싱글 노드. 향후 CephFS로 무중단에 가깝게 전환 가능해야 함
- 워크로드는 **읽기 위주**. 동시 쓰기는 드물다고 전제
- 게이트웨이는 당분간 1대 운영
- **사내망/VPN 한정 운영. 인터넷에 직접 노출하지 않는다** — [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)가 MFA를 포기하는 전제이므로, 이 항목이 바뀌면 인증 결정을 다시 열어야 한다

### 명시적 비목표 (Non-goals)

- 동시 쓰기 조정(락, 임대, 충돌 병합) — [ADR-6](#adr-6-동시-쓰기--last-write-wins) 참조
- SMB/NFS 프로토콜 자체 구현
- 실시간 협업 편집
- **MFA / SSO** — 위 사내망 전제 하에 의도적으로 포기. 근거와 뒤집는 조건은 [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)
- **디렉터리 서비스가 되는 것** — 계정은 [usersync](#adr-8-계정-생명주기는-usersync에-위임)가, 장차 AD가 관리한다

---

## 3. 아키텍처

```
[브라우저]                    [탐색기 / Finder]
    │ HTTPS                        │ SMB
    ▼                              ▼
┌─────────────────┐        ┌──────────────┐
│  Go 서버 (root) │        │    Samba     │
│  웹 UI · 관리   │        │  (호스트)     │
└────────┬────────┘        └───────┬──────┘
         │ Unix socket              │
         │ + SCM_RIGHTS (fd)        │
         ▼                          │
┌─────────────────┐                 │
│ 유저별 헬퍼 프로세스 │            │
│ (setpriv로 강등)  │              │
└────────┬────────┘                 │
         ▼                          ▼
   ┌────────────────────────────────────┐
   │   POSIX 파일시스템                  │
   │   초기: 로컬 XFS  →  이후: CephFS   │
   └────────────────────────────────────┘
```

### 컴포넌트 역할

**Go 서버** — 웹 UI, 인증, 공유 관리, 검색, 감사 로그, 썸네일. 파일 I/O는 헬퍼가 넘겨준 fd로 수행.

**헬퍼 프로세스** — 사용자별로 1개. 해당 사용자 권한으로 실행되며 경로를 열어 fd만 반환.

**Samba** — SMB 프로토콜과 그에 따른 임퍼소네이션 전담. Go 서버는 관여하지 않음. `tdbsam`이 **웹·SMB 공통 자격증명 저장소**이며, 웹은 `ntlm_auth`로 여기에 검증을 위임한다([ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)).

**usersync** — 계정·그룹·tdbsam 항목의 생명주기. 서비스가 아니라 관리자가 돌리는 CLI이며, `roster.yaml`이 선언이다. 파일서버는 계정을 만들지 않는다([ADR-8](#adr-8-계정-생명주기는-usersync에-위임)).

---

## 4. 결정 사항 (ADR)

### ADR-1: 권한 모델은 POSIX uid/gid

**결정.** Unix 사용자/그룹/mode/ACL을 유일한 인가 체계로 사용한다.

**근거.** Samba는 SMB 인증 후 결국 Unix uid로 매핑해 파일에 접근한다. 즉 **SMB를 제공하기로 한 시점에 Unix 계정 체계는 이미 필수 요건이다.** 웹 UI만 별도 권한 모델을 쓰면 같은 데이터에 진실의 원천이 둘 생기고, 두 모델은 반드시 어긋난다.

**결과.** 셀프서비스 공유(임의의 사용자에게 임의 폴더 읽기 권한 부여)는 POSIX 그룹 폭발 문제를 낳는다. → [미결정 사항](#10-미결정-사항) 참조.

---

### ADR-2: 인증은 tdbsam 하나 (SSO 미채택)

**결정.** 인증(누구인가)은 **Samba의 `tdbsam` 하나**를 쓴다. 웹 서버는 `ntlm_auth`를 호출해 같은 저장소에 검증을 위임한다. 인가(무엇을 할 수 있나)는 파일시스템. **비밀번호는 사용자당 하나이고, 웹과 SMB가 그것을 공유한다.**

**근거.**

- **SMB는 어차피 SSO를 못 쓴다.** SMB2/3의 인증은 SPNEGO로 감싼 Kerberos 또는 NTLMSSP뿐이고 OIDC 토큰이 들어갈 자리가 없다. KDC가 없는 지금 SMB는 무조건 자기 자격증명을 쓴다. **SMB의 SSO는 Kerberos가 유일한 답이고, 그건 AD가 와야 생긴다.**
- **사내 IdP(Entra)가 전 직원을 커버하지 않는다.** 최저 등급이고 사실상 이메일 전용이라 계정이 없는 인원이 있다. 그들은 OIDC로 로그인할 수단 자체가 없다.
- **그래서 SSO는 도움이 가장 덜 필요한 층을 돕는다.** 매일 이메일을 쓰는 사람에겐 편하지만, [2장](#2-요구사항)이 명시적으로 우선한 "컴퓨터에 익숙하지 않은 사용자"는 대체로 그 반대편에 있다.
- **웹만 SSO를 붙이면 진실의 원천이 둘이 된다.** [설계 원칙 1](#설계-원칙)과 정면으로 어긋난다. 실무적으로는 오프보딩이 깨진다 — IdP만 잠그면 **웹은 막히는데 SMB 비밀번호는 살아 있는** 상태가 조용히 남는다. 단일 저장소면 `roster.yaml`의 `status: disabled` 한 줄로 둘 다 죽는다([ADR-8](#adr-8-계정-생명주기는-usersync에-위임)).
- PAM은 cgo 의존이 생겨 배포가 번거롭다. `ntlm_auth`는 exec 호출이라 정적 바이너리를 유지한다.

**대가: MFA가 없다.** 이 구성에서 실질적으로 포기하는 것은 이것 하나다. **수용 조건을 명시한다 — 서버를 인터넷에 직접 노출하지 않고 사내망/VPN 한정으로 운영한다.** 이 전제가 깨지면 결정을 다시 연다.

**결정을 뒤집는 조건.** ⓐ 외부 노출이 필요해짐, ⓑ 온프렘 AD 도입으로 전 직원이 도메인 계정을 갖게 됨, ⓒ Entra 커버리지가 전원으로 확대됨. 그래서 **웹 인증은 `Authenticator` 인터페이스 뒤에 두고 구현은 `ntlm_auth` 하나만** 둔다.

**AD가 오면 공짜로 승계된다.** 도메인 조인 후 같은 `ntlm_auth` 호출이 winbind를 경유해 AD를 인증한다 — **웹 인증 코드는 한 줄도 안 바뀐다.** 이것이 이 선택의 실질적 이득이다.

**운영 전제.** `ntlm_auth`는 winbindd를 경유한다. standalone Samba에서는 winbindd를 안 띄우는 경우가 많으므로 **웹 인증을 붙이기 전에 실행 상태로 만들어야 한다.** 어차피 AD 조인 후엔 필수이므로 지금 켜 두면 전환 시 바뀔 것이 하나 줄어든다. 정확한 호출 형태와 권한 요건(root 또는 `winbindd_priv` 그룹)은 배포 대상 Samba 버전에서 실측할 것.

---

### ADR-3: 임퍼소네이션은 헬퍼 프로세스 + fd 패싱

**결정.** 사용자별 헬퍼 프로세스를 `setpriv --reuid --regid --init-groups`로 강등해 띄우고, 유닉스 소켓으로 open 요청을 받아 `SCM_RIGHTS`로 fd를 전달한다.

**검토한 대안과 탈락 사유:**

| 대안 | 탈락 사유 |
|---|---|
| `syscall.Setuid` | Go 런타임이 모든 스레드에 브로드캐스트. goroutine별 분리 불가 |
| `setfsuid` + `LockOSThread` | 동작은 하나 원복 누락 시 위험. 보조 그룹 처리 등 미묘한 부분 많음 |
| root로 접근 + 앱이 권한 검사 | mode bits·보조 그룹·ACL·sticky bit를 재구현해야 함. 틀리면 곧 보안 사고 |
| 요청마다 `su -c` | PAM 세션 스택 전체 실행(wtmp, logind scope 생성). 무겁고 로그 오염. 셸 경유로 인젝션 위험 |
| 요청마다 `setpriv` fork | fork/exec 비용이 요청마다 발생. 파이프 경유라 Range·seek 곤란 |
| 권한 "확인"만 자식으로, 읽기는 root로 | **TOCTOU.** 검사와 접근이 같은 권한 컨텍스트에서 원자적으로 일어나야 함 |

**이점.** fd에 권한 검사 결과가 박혀 있어 TOCTOU가 없다. `io.Copy`로 sendfile 경로를 탈 수 있다. Samba가 20년간 검증한 구조다. 유휴 헬퍼는 타임아웃으로 회수.

#### ⚠️ 헬퍼 프로토콜의 범위 — 이 설계 최대의 함정

**fd 패싱이 커버하는 것은 "그 파일의 read/write"뿐이다.** `rename`·`unlink`·`mkdir`·`link`·`O_CREAT`는 **디렉터리 쓰기 권한을 호출 프로세스의 자격증명으로 매번 새로 검사**한다. 헬퍼가 넘겨준 dirfd로 `renameat(dirfd, …)`을 해도 마찬가지고, **호출자가 root면 DAC를 전부 통과한다.**

즉 헬퍼 역할을 "경로를 열어 fd만 반환"으로 두면, [6장 쓰기 규약](#6-파일-쓰기-규약)의 4단계 중 Go 서버가 직접 해도 되는 것은 **내용 write 하나뿐**이다. 나머지를 root 서버가 수행하면 권한 검사가 통째로 사라지고, 위 표에서 탈락시킨 *"root로 접근 + 앱이 권한 검사"* 로 그대로 회귀한다.

**따라서:**

1. 헬퍼 프로토콜은 `OPEN`이 아니라 **오퍼레이션 집합**이다 — `OPEN` / `OPENAT_CREATE` / `MKDIR` / `RENAME` / `LINK` / `UNLINK` / `RMDIR` / `STAT` / `CHMOD` / `SETXATTR` / `READDIR`. Phase 0의 "프로토콜 설계"는 여기서 말하는 것보다 훨씬 큰 작업이다.
2. **불변식: Go 서버는 경로 기반 syscall과 `*at()` 계열을 절대 직접 호출하지 않는다.** 서버가 만지는 것은 헬퍼가 넘겨준 fd에 대한 `read`/`write`/`pread`/`sendfile`/`getdents`뿐. 코드 리뷰 체크리스트나 lint 규칙으로 강제할 가치가 있다.
3. **[ADR-4](#adr-4-경로-안전성은-커널에-위임)의 `openat2` 래퍼는 서버가 아니라 헬퍼 안에 있어야 한다.** 경로 해석 자체가 사용자 자격증명으로 일어나야 한다.

#### 헬퍼 수명 주기 — 아직 비어 있는 부분

- **요청 다중화**가 없다. 소켓 위 단순 req/resp면 head-of-line blocking이 생긴다(큰 업로드 하나가 그 사용자의 디렉터리 목록 조회를 막음). 요청 ID 기반 멀티플렉싱이나 소규모 풀이 필요하다.
- `setpriv --init-groups`는 **기동 시점의 보조 그룹을 고정**한다. 팀에 새로 추가된 사용자는 헬퍼가 회수될 때까지 접근이 안 된다. 그룹 변경 시 해당 헬퍼를 강제 재기동하는 경로가 필요하다.
- 사용자 수 × 헬퍼 = 프로세스/fd 리소스. 상한과 LRU 회수 정책을 정할 것.
- 소켓은 root-only 디렉터리에 두고 `SO_PEERCRED`로 검증.

#### 서버가 root일 필요가 있는가

서버에 root가 필요한 이유는 **헬퍼를 `setpriv`로 띄우기 위해서** 하나뿐이다. 그런데 서버는 HTTP 파싱·썸네일 생성(libvips/ImageMagick — 전통적인 RCE 벡터)·검색 인덱싱까지 떠안는 **이 시스템 최대의 공격 표면**이다.

→ 아주 작은 root 전용 **spawner 데몬**("이 uid로 헬퍼 하나 띄워서 소켓 fd를 돌려줘"만 하는 수백 줄)을 두고 Go 서버는 비특권으로 실행하는 구조를 검토할 것. 그러면 [ADR-4](#adr-4-경로-안전성은-커널에-위임)의 "root라서 path traversal 하나가 전체 유출" 전제가 약해진다(그래도 `openat2`는 유지). 썸네일 생성은 별도 seccomp 샌드박스 워커로 빼는 것을 강력히 권한다.

---

### ADR-4: 경로 안전성은 커널에 위임

**결정.** 문자열 기반 경로 검증을 하지 않는다. `openat2(2)`에 `RESOLVE_BENEATH | RESOLVE_NO_MAGICLINKS`를 지정한다. Go 1.24의 `os.Root` / `os.OpenInRoot` 또는 `golang.org/x/sys/unix.Openat2` 사용.

**근거.** 서버가 root로 동작하므로 path traversal 한 건이 곧 전체 파일시스템 유출이다. 심볼릭 링크·`..`·경합 조건을 애플리케이션이 전부 막는 것은 비현실적이다.

---

### ADR-5: SMB는 Samba에 위임, uid 매핑은 문제가 아님

**결정.** SMB 프로토콜을 구현하지 않는다. 호스트에 Samba를 띄운다.

**정리해둘 사실.** SMB에는 uid가 흐르지 않는다. 세션 기반 인증 프로토콜이며, 서버가 인증된 사용자로 파일 작업을 수행하고 결과만 반환한다. 클라이언트는 서버 측 uid를 알지도, 보내지도 않는다.

- Windows / macOS: uid 개념 자체가 없음
- Linux(cifs): 마운트 옵션 `uid=`/`gid=`로 지정한 값으로 **표시만** 됨. 로컬 눈속임이며 실제 권한과 무관. 필요 시 `noperm`으로 클라이언트 측 검사 비활성화

즉 서버에서 개인 디렉터리 소유자가 1047번이든 5000번이든 클라이언트 경험에 영향이 없다. **권한 검사는 100% 서버에서 일어난다.**

---

### ADR-6: 동시 쓰기 — Last Write Wins

**결정.** 락 매니저를 구현하지 않는다. lost update를 허용한다.

**근거.** 워크로드가 읽기 위주다. 락 소유자 크래시 복구, 임대 갱신, 클러스터 전역 락 상태 관리 등 복잡도 덩어리 전체를 제거할 수 있다.

**단, 아래 두 가지는 포기하지 않는다.** LWW의 실제 위험은 충돌 자체가 아니라 **손실이 조용하다는 것**이다.

1. **원자적 교체** — torn file 방지. 원자성 문제이지 동시성 문제가 아니며 비용이 사실상 0
2. **휴지통 경유 덮어쓰기** — 날아간 작업을 항상 복구 가능하게

→ [파일 쓰기 규약](#6-파일-쓰기-규약) 참조.

**알려진 구멍.** SMB 세션의 share mode를 Go 서버가 인지하지 못한다. 누군가 엑셀로 열어둔 파일을 웹에서 덮어쓸 수 있다. `kernel share modes = yes` + `flock(LOCK_EX|LOCK_NB)`으로 감지 가능하나 성능 비용이 있고 CephFS 동작 확인이 필요하다. 발생 빈도가 낮고 휴지통으로 복구되므로 **당분간 수용**한다.

> ⚠️ **위 "휴지통으로 복구되므로" 근거는 절반만 맞다.** 웹이 덮어쓴 경우에만 성립하고, **SMB에서 in-place로 덮어쓴 것은 어느 쪽으로도 복구되지 않는다.** LWW를 수용 가능하게 만드는 안전망이 한쪽 경로에만 있다는 뜻이다 → [10장](#-데이터-보호가-웹-경로에만-있다).

---

### ADR-7: 스토리지는 CephFS 전환을 전제로 설계

**결정.** 초기에는 로컬 파일시스템, 이후 CephFS로 전환. Go 서버 코드는 POSIX 경로만 다루므로 변경 없음.

**CephFS의 uid 모델 (오해 정리).** CephFS는 로컬 파일시스템과 동일하게 inode에 uid/gid를 **정수로** 저장한다. Ceph 내부에 사용자 DB는 없다. 신뢰 모델은 NFSv3과 같아서, 마운트가 허용된 머신의 root는 사실상 임의 uid를 사칭할 수 있다.

요구사항은 하나뿐이다: **마운트하는 모든 노드가 uid 번호를 동일하게 해석할 것.**

그런데 **직원 단말은 CephFS를 마운트하지 않는다.** 웹 또는 SMB로 들어온다. CephFS를 마운트하는 건 관리 대상 게이트웨이 서버 몇 대뿐이므로, uid 일치 범위가 회사 전체가 아니라 서버 2~3대다.

**그 일치를 보장하는 것이 [usersync](#adr-8-계정-생명주기는-usersync에-위임)다.** `roster.yaml`이 번호의 정본이므로 게이트웨이마다 같은 roster로 `apply`하면 uid가 일치한다. 온프렘 AD 도입 후에는 NSS가 그 역할을 넘겨받고, usersync는 `mode: audit`으로 **번호가 여전히 일치하는지 감시하는** 쪽으로 물러난다.

필요 시 MDS caps로 경로를 제한할 수 있다. **경로는 CephFS 네임스페이스 안의 경로**이지 호스트 마운트 지점이 아니다 — `/srv/data`는 우리가 붙이는 자리이므로 여기 쓰면 존재하지 않는 서브트리를 허용하게 된다:
```
ceph fs authorize cephfs client.gw / rw          # 파일시스템 루트 전체
# 또는 CephFS 안에 /data 서브트리를 두고 그것만:
ceph fs authorize cephfs client.gw /data rw
```

**단, uid를 고정하는 caps는 이 게이트웨이에 쓸 수 없다.**
```
caps mds = "allow rw path=/home uid=1000 gids=1000,1001"   # ← 게이트웨이에는 부적합
```
이 형태는 **매칭 필터**다 — 클라이언트가 보낸 `caller_uid`/`caller_gid`가 cap에 적힌 값과 다르면 어떤 grant도 매칭되지 않아 MDS가 권한 오류를 돌려준다. 소유자를 1000번으로 바꿔 쓰는(rewrite/squash) 것이 아니라 **그 외의 uid로 하는 연산을 전부 거부**하는 것이다.

결과적으로 게이트웨이에는 쓸 수 없다는 결론은 같다. 게이트웨이는 정의상 여러 uid를 번갈아 사칭해야 하므로(그게 [ADR-3](#adr-3-임퍼소네이션은-헬퍼-프로세스--fd-패싱)의 전부다), 이 caps를 걸면 지정한 한 사람 외의 모든 요청이 실패한다. 게이트웨이에는 **경로 제한만** 걸고 uid 제한은 걸지 않는다.

**`root_squash`도 현 구조와 충돌한다.** Go 서버가 root로 돌면서 팀 디렉터리 프로비저닝(`chown`/`chgrp`)을 하는 한 그 작업이 막힌다. 다만 [ADR-3의 spawner 분리](#서버가-root일-필요가-있는가)를 채택해 서버를 비특권화하면 오히려 켤 수 있게 된다 — 파일 I/O는 전부 비-root 헬퍼가 하기 때문이다. 두 결정은 연동되어 있다.

---

### ADR-8: 계정 생명주기는 usersync에 위임

**결정.** Unix 계정·그룹·tdbsam 항목의 생성/비활성/은퇴는 [**usersync**](https://github.com/lesomnus/usersync)가 관리한다. `roster.yaml`이 선언이고 `usersync apply`가 수렴시킨다. 파일서버는 계정을 직접 만들지 않는다.

**근거.** [ADR-1](#adr-1-권한-모델은-posix-uidgid)이 POSIX를 유일한 인가 체계로 정한 순간, 이 시스템에는 "누가 uid를 발급하고 회수하는가"라는 장부가 필요해진다. usersync는 이미 그것이고, 특히 **uid 재사용 방지**를 정면으로 다룬다 — 은퇴자를 지우는 대신 `status: reserved` tombstone으로 남겨 번호를 영구 점유한다. 이 문제는 자체 구현했다면 반드시 나중에 사고로 배웠을 것이다.

**ID 대역.** `usersync.yaml`의 `manage` 창이 정본이다.

| 대역 | 용도 |
|---|---|
| `3000`–`9999` | 사용자 uid. UPG의 gid가 uid와 같으므로 이 번호대는 gid로도 소비된다 |
| `10000`–`19999` | 팀 그룹 gid |

**→ 온프렘 AD 도입 시 IT에 `3000–19999` 대역 예약을 문서로 요청해야 한다.** 예약은 일회성 협상이고 번호는 공짜인 반면, **재번호는 사실상 불가능**하다 — 라이브 파일은 `chown -R`로 고쳐도 스냅샷은 불변이라 못 고치고, 과거 스냅샷이 영구히 틀린 uid로 남는다. 그래서 현 인원보다 넉넉하게 잡았다.

**AD 전환.** 온프렘 AD 도입 예정이나 **시기 미정**. 전환은 "uid 번호를 누가 답하는가"가 `/etc/passwd`에서 AD로 넘어가는 것 하나이고, `idmap_ad` + RFC2307로 **같은 번호를 그대로 심으면 chown·데이터 이동 0회**다. usersync가 이를 위해 `export --format csv|ldif`(번호 주입용 출력), `detach`(로컬 계정만 놓아주고 홈은 보존), `mode: audit` + `audit`(인계 후 장부 감시)를 제공한다. 상세는 usersync 저장소의 `identity-roadmap.md`.

> ⚠️ **Samba는 Entra ID(클라우드)에 도메인 조인할 수 없다.** `net ads join`이 되는 대상은 진짜 AD DS뿐이다. 도입 대상이 온프렘 AD DS인지 확인이 필요하며, 아니라면 Kerberos SMB SSO는 영원히 오지 않고 [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)의 tdbsam 구조가 영구가 된다.

**결과.** 아래 5장의 디렉터리 레이아웃·Samba 공유 블록은 **usersync가 생성한다**(`usersync shares --write`). 이 문서의 예시는 그 출력과 일치해야 하며, 어긋나면 usersync 쪽이 정본이다.

---

## 5. 권한 레이아웃

### 디렉터리 구조

```
/srv/data/
├── homes/
│   └── <username>/          0700, owner=<user>
└── teams/
    └── <teamname>/          2770, group=team-<teamname>
```

### 팀 디렉터리 초기화

```bash
mkdir -p /srv/data/teams/design
chgrp -R team-design /srv/data/teams/design
chmod -R g+rwX      /srv/data/teams/design
find /srv/data/teams/design -type d -exec chmod g+s {} +   # setgid는 디렉터리에만
```

**setgid 비트가 핵심이다.** 이게 없으면 하위에 생성되는 파일이 생성자의 기본 그룹을 따라가 팀원이 수정할 수 없게 된다.

> **`chmod -R g+ws`를 쓰지 말 것.** `-R`은 일반 파일에도 setgid를 붙인다(`X`와 달리 `s`에는 디렉터리 전용 예외가 없다). 의도한 것은 디렉터리의 setgid이므로 위처럼 분리한다. (`g+rwX`의 대문자 `X`는 이미 실행 비트가 있는 것과 디렉터리에만 `x`를 준다.)
>
> 일반 파일의 setgid는 **아무 효과도 없다** — 예전에 mandatory locking 마커로 쓰이긴 했으나 그 기능은 Linux 5.15에서 제거됐고, 그전에도 `-o mand` 마운트가 필요했으며 XFS 기본 마운트나 CephFS는 지원한 적이 없다. 문제는 위험해서가 아니라 **의미 없는 비트가 남아 보안 스캐너가 매번 걸고 넘어지고, 읽는 사람이 의도를 오해한다**는 것이다.

### Samba 공유 설정

> 이 블록은 [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)에 따라 `usersync shares --write`가 생성한다. 아래는 그 출력과 일치해야 하는 참조본이다.

```ini
[homes]
   path = /srv/data/homes/%U       # ← 필수. 없으면 passwd의 홈을 쓴다
   browseable = no
   read only = no
   valid users = %S
   create mask = 0600
   directory mask = 0700

[team-design]
   path = /srv/data/teams/design
   valid users = @team-design
   force group = team-design
   create mask = 0660
   directory mask = 2770
   force directory mode = 2770
```

**mode 산술을 정확히 알고 써야 한다.** Samba는 `(base & mask) | force`로 새 객체의 mode를 정하는데, **SMB에는 유닉스 mode가 흐르지 않으므로** `base`는 클라이언트 요청이 아니라 **smbd가 DOS 속성에서 유도한 값**이다 — 파일 `0666`(archive 속성이 owner-execute로 매핑되면 `0766`), 디렉터리 `0777`.

| 설정 | 파일 | 폴더 | 판단 |
|---|---|---|---|
| 없음(전역 기본 `0744`/`0755`) | `0744` | `2755` | group·other 비트가 남는다 |
| `create mask = 0660` / `directory mask = 2770` | `0660` | `2770` | **이것만으로 충분하다** |
| + `force create mode = 0660` | `0660` | — | **차이 없음.** 넣지 않는다 |
| mask가 force보다 넓음 (`0664` + `0660`) | `0664` | — | **world-readable**이 조용히 남는다 |

**`force directory mode = 2770`만은 실제로 필요하다.** mask는 비트를 **지우기만** 하므로 setgid를 되돌릴 수 없다. 부모 디렉터리가 setgid를 잃으면(드리프트) 그 아래 SMB로 만든 폴더가 `0770`·setgid 없음으로 떨어지고, **그 안에 생성되는 파일이 생성자의 기본 그룹을 따라가** 팀원이 수정하지 못한다 — 한 단계 아래에서 벌어지고 클라이언트에는 단서가 없다. force가 어느 깊이에서든 setgid를 복원한다.

> 위 숫자는 추론이 아니라 **실제 smbd로 측정한 값**이다. usersync 저장소의 `scripts/verify-samba-modes.sh`가 재현한다(부모 `2770`: mask만으로 `0660`/`2770` — force 무의미. 부모 `0770`: mask만 `0770`, force 있으면 `2770`).

**`[homes]`의 `path`도 필수다.** Samba의 `[homes]`는 `path`가 없으면 `getpwnam()`의 홈 디렉터리를 쓴다. 계정 프로비저닝이 passwd의 홈을 `/srv/data/homes/<user>`로 잡아준다면 생략해도 맞지만, 그 일치에 암묵적으로 기대는 대신 명시한다.

### ⚠️ 웹 경로도 동일한 mode로 생성해야 함

누락 시 *"웹으로 올린 파일만 팀원이 수정 못 하는"* 버그가 생기며, 사용자는 원인을 짐작조차 못 한다. 두 가지를 지킬 것:

1. **umask를 전역 고정하지 말 것.** 홈은 `0077`, 팀은 `0002`로 **대상 트리마다 다르다.** `0002`로 고정하면 홈에 웹으로 올린 파일이 `0664`가 되어 SMB 경로(`0600`)와 어긋난다 — 홈이 `0700`이라 실제 유출은 없지만 *"두 경로가 동일한 권한 규칙"*이라는 요구사항이 이미 깨진다.
2. **더 안전한 방법은 umask에 기대지 않는 것이다.** umask는 프로세스 전역 상태라 동시 요청을 처리하는 헬퍼에서 레이스가 난다. 헬퍼가 생성 직후 `fchmod`로 목표 mode를 명시적으로 박는 편이 낫다.

---

## 6. 파일 쓰기 규약

모든 쓰기는 아래 순서를 따른다. **예외 없음.**

```go
// 1. 같은 디렉터리에 임시 파일 생성 (mode/group 규칙 적용)
tmp := filepath.Join(dir, ".upload-"+random)

// 2. 내용 기록 후 fsync(파일)

// 3. 기존 파일이 있으면 휴지통에 하드링크로 고정 (target은 그대로 살아있다)
os.Link(target, trashPath)     // .trash/2026-08-08T13:22_report.xlsx

// 4. 원자적 교체 — 단 한 번
os.Rename(tmp, target)

// 5. fsync(부모 디렉터리)
```

**왜 이 순서인가**

- `rename(2)`는 원자적이다. 읽는 쪽은 항상 옛 파일 또는 새 파일만 보며, 중간 상태를 절대 보지 않는다. CephFS도 같은 디렉터리 내에서 보장한다
- 업로드가 중단되면 임시 파일만 남고 원본은 무사하다
- 3번 덕분에 LWW로 덮어쓴 작업이 **항상 복구 가능**하다. 사실상 버전 관리이며 구현 비용은 링크 한 번. 휴지통 기능이 덤으로 생긴다

**왜 `rename(target→휴지통)`이 아니라 `link`인가.** rename 두 번이면 그 사이에 **target이 존재하지 않는 창**이 열린다. 그때 읽던 클라이언트는 ENOENT를 받는다 — *"읽는 쪽은 항상 옛 파일 또는 새 파일만 본다"*는 위 주장이 실제로는 성립하지 않게 된다. `link`는 옛 inode를 휴지통에 붙들어두면서 target을 살려두므로, 교체가 **단 한 번의 원자적 rename**으로 끝나고 빈 창이 없다. `link`도 `rename`과 같이 같은 파일시스템 내여야 하므로 제약이 늘지 않는다. 다만 CephFS의 하드링크는 backtrace 갱신 비용이 있으니 성능은 측정할 것.

**주의 (정정).** `rename`은 새 inode를 만들지 않는다 — 기존 디렉터리 엔트리가 임시 파일의 inode를 가리키도록 옮길 뿐이다. 결과적으로 **소유권·mode가 임시 파일 것을 따르는 것은 맞다.** 또한 setgid 디렉터리가 상속시키는 것은 **그룹뿐이고 mode는 상속되지 않으므로**, [5장](#-웹-경로도-동일한-mode로-생성해야-함)의 mode 규칙을 임시 파일 생성 시점에 명시적으로 적용해야 한다.

**5번(디렉터리 fsync)이 빠지면 안 된다.** 파일만 fsync하면 크래시 시 디렉터리 엔트리가 유실될 수 있다.

**신규 생성 경로**에는 `renameat2(RENAME_NOREPLACE)`를 쓰면 *"덮어쓰기 / 사본 저장"* 분기가 깔끔해진다. CephFS의 `renameat2` 플래그 지원 범위는 확인이 필요하다.

### 휴지통 위치

**권한 도메인마다 그 도메인 루트에 둔다** — `teams/design/.trash/`, `homes/alice/.trash/`.

팀 파일을 개인 홈 밑 휴지통으로 보내면 **다른 팀원이 복원할 수 없고**, 데이터가 팀 권한 도메인 밖으로 나간다. 반대로 팀 디렉터리 안에 두면 SMB에서 그대로 보이므로 Samba의 `veto files` / `hide files`로 가린다. 쿼터 정산 대상에 포함할지도 함께 결정할 것([10장](#10-미결정-사항)).

**정리 작업.** 30일 경과한 휴지통 항목 삭제 cron. 고아 임시 파일(`.upload-*`) 정리 cron.

---

## 7. 확장성과 이후 경로

### 스토리지 확장 (단일 → CephFS)

**난이도: 낮음.** 마운트 지점 교체만으로 완료. Go 서버 코드 변경 없음.

체감 차이는 **지연 시간 프로파일**에서 발생한다.

- 메타데이터 연산이 MDS로 네트워크 왕복 → `stat`이 마이크로초에서 밀리초 단위로
- Windows 탐색기는 stat-heavy(항목별 아이콘·썸네일·크기 조회). 대용량 디렉터리에서 체감
- 작은 파일 대량 쓰기는 느려짐(왕복 + 복제본 확정 대기)
- 반대로 큰 파일 순차 전송은 OSD 병렬 분산으로 **더 빠를 수 있음**

사무용 파일 공유 워크로드에서는 MDS 캐시가 잘 들어 대체로 수용 가능한 수준.

### 게이트웨이 다중화

**난이도: 높음. 여기가 진짜 프로젝트다.**

SMB의 상태 — 락, oplock/lease, share mode — 는 파일시스템이 아니라 **Samba 프로세스의 메모리와 tdb**에 있다. 노드 2대가 되면 서로의 락을 모른다. 서로 다른 게이트웨이로 접속한 두 사용자가 같은 파일을 열면 둘 다 쓰기 권한을 얻는다.

해법은 **CTDB**(clustered TDB). Ceph 위 Samba의 표준 구성이지만 recovery lock 공유 저장소, 노드 간 하트비트, public IP 페일오버가 따라붙는다.

**단, 스토리지가 커진다고 게이트웨이가 자동으로 부족해지지는 않는다.** 사내 규모에서는 1대로 상당 기간 운영 가능하다.

### 지금 피해둘 것 (CephFS 호환성)

| 하지 말 것 | 대신 |
|---|---|
| `fallocate` 사전 할당 후 offset 쓰기 | 청크를 별도 파일로 쌓았다가 마지막에 병합 |
| SQLite 등 앱 DB를 데이터 볼륨에 배치 | 로컬 또는 별도 DB. 게이트웨이 다중화 시에도 이 분리가 유효 |
| 락 파일 생성/존재 확인 방식 | `flock` / `fcntl` (CephFS가 클러스터 전역 지원) |
| inode 번호를 파일 식별자로 사용 | 자체 ID 발급 후 xattr 또는 DB로 관리 |

---

## 8. 읽기 경로 최적화

동시성에서 아낀 예산을 여기 투입한다.

- **조건부 GET** — `If-None-Match` / `If-Modified-Since`
- **Range 요청** — 영상 스크럽, 다운로드 재개에 필수
- **썸네일 캐시** — 원본이 아닌 별도 저장소에
- **디렉터리 목록** — CephFS에서 항목별 `stat`은 비싸다. `getdents`로 일괄 조회 후 필요한 필드만 채우거나, 목록을 짧게 캐싱

---

## 9. 구현 계획

### Phase 0 — 기반

> **이 단계가 이 프로젝트에서 가장 무거운 항목이다.** [ADR-3의 프로토콜 범위](#-헬퍼-프로토콜의-범위--이-설계-최대의-함정) 참조 — 불변식 하나만 놓쳐도 ADR-3이 배제한 보안 사고가 그대로 돌아온다.

- [ ] 헬퍼 **오퍼레이션 집합** 설계 (유닉스 소켓 + `SCM_RIGHTS`) — `OPEN`/`OPENAT_CREATE`/`MKDIR`/`RENAME`/`LINK`/`UNLINK`/`RMDIR`/`STAT`/`CHMOD`/`SETXATTR`/`READDIR`
- [ ] 요청 다중화 (head-of-line blocking 방지)
- [ ] `setpriv` 기반 헬퍼 기동 / 유휴 회수 / 그룹 변경 시 강제 재기동
- [ ] `openat2(RESOLVE_BENEATH)` 래퍼 — **헬퍼 안에** 배치
- [ ] 불변식 강제: 서버는 경로 기반 syscall·`*at()`을 직접 호출하지 않음 (lint 또는 리뷰 체크리스트)
- [ ] mode 규칙 적용 지점 확정 (umask 아닌 명시적 `fchmod` 권장)
- [ ] usersync 배포 + `roster.yaml` 부트스트랩 ([ADR-8](#adr-8-계정-생명주기는-usersync에-위임))
- [ ] winbindd 기동 (`ntlm_auth` 전제, [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택))

### Phase 1 — 최소 동작

- [ ] 인증 — `ntlm_auth` → tdbsam. `Authenticator` 인터페이스 뒤에 배치
- [ ] 디렉터리 목록 / 다운로드 (Range 포함)
- [ ] 업로드 (임시 파일 → fsync → link → rename → dir fsync)
- [ ] 휴지통 (덮어쓰기 시 자동, 수동 복원 UI)
- [ ] 디렉터리 레이아웃 및 Samba 공유 구성 (`usersync shares --write`)

### Phase 2 — 운영 편의

- [ ] 웹 UI 마감 (모바일 포함, 비숙련 사용자 기준으로 검증)
- [ ] 공유 링크 발급 (만료·비밀번호) — ⚠️ [ADR-1 예외](#-공유-링크와-셀프서비스-공유는-같은-결정이다)
- [ ] 감사 로그 — 웹 + **SMB(`vfs objects = full_audit`)** 양쪽
- [ ] SMB 삭제 휴지통 (`vfs objects = recycle`) — [경로 비대칭](#-데이터-보호가-웹-경로에만-있다) 해소
- [ ] 썸네일 / 미리보기 — **비특권 seccomp 워커로 분리**
- [ ] 휴지통·고아 임시 파일 정리 cron
- [ ] 백업/스냅샷 정책 수립 — 휴지통은 백업이 아니다
- [ ] `usersync audit` 정기 실행 (계정 드리프트 감시)

### Phase 3 — 확장

- [ ] CephFS 전환
- [ ] 검색 인덱싱
- [ ] ETag + `If-Match` 기반 덮어쓰기 경고 (409 → "덮어쓰기 / 사본 저장" 선택)
- [ ] (필요 시) 게이트웨이 다중화 + CTDB

---

## 10. 미결정 사항

### ✅ SMB 비밀번호 — 해소됨

> **결정: 자격증명 저장소를 tdbsam 하나로 통일한다.** 웹은 `ntlm_auth`로 같은 것을 검증한다. 사용자당 비밀번호는 하나이고, 웹용/SMB용이 갈릴 여지가 없다. 근거·대가·뒤집는 조건은 [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택).
>
> 이전에 검토했던 `smbpasswd -s` 동기화는 **웹이 SSO를 쓸 때만 필요한 우회**였으므로 함께 폐기된다. AD 도메인 참여는 여전히 옳은 최종 형태이지만 **도입 시기가 미정**이라 지금 기다릴 수 없다 → [ADR-8](#adr-8-계정-생명주기는-usersync에-위임).

### 🔴 공유 링크와 셀프서비스 공유는 같은 결정이다

두 항목이 지금까지 갈라져 있었으나 **본질이 같다 — 둘 다 POSIX 바깥의 앱 레벨 권한이다.**

- **셀프서비스 공유** — "이 폴더를 김대리에게 읽기 전용으로"를 사용자가 직접 처리하게 하려면 POSIX 그룹이 폭발한다. POSIX ACL로 가능하나 API로 관리하기 번거롭다.
- **공유 링크 발급**([Phase 2](#phase-2--운영-편의)) — 미인증 요청자에게 파일을 내주는 기능이다. **어떤 uid로 읽는가?** 발급자의 uid다. 이것은 [ADR-1](#adr-1-권한-모델은-posix-uidgid)의 명백한 예외다.

그런데 하나는 미결정, 하나는 Phase 2 확정 기능으로 갈라져 있었다. **하나의 의사결정으로 묶어서 결정할 것.**

**→ 셀프서비스 공유가 실제로 필요한지 먼저 확인.** 필요 없다면 공유 링크만 "ADR-1의 명시적·유일한 예외"로 좁게 정의하고 감사 로그로 보완한다. 필요하다면 소유권·기본 권한만 Unix에 맡기고 공유는 앱 레벨 ACL로 두는 하이브리드를 검토(단 [설계 원칙 1](#설계-원칙)과 상충하므로 신중히).

### 🔴 데이터 보호가 웹 경로에만 있다

핵심 안전망이 **전부 Go 서버 쪽에만** 있어 두 경로가 비대칭이다. [2장](#2-요구사항)의 *"동일한 데이터, 동일한 권한 규칙"* 요구를 진지하게 지키려면 메우거나 ADR로 명시해야 한다.

| | 웹 | SMB |
|---|---|---|
| 삭제 시 휴지통 | ✅ | ❌ → `vfs objects = recycle` |
| 덮어쓰기 시 휴지통 | ✅ | ❌ **커버 불가** (in-place write) |
| 감사 로그 | ✅ | ❌ → `vfs objects = full_audit` |

**[ADR-6](#adr-6-동시-쓰기--last-write-wins)의 "알려진 구멍" 판단을 정정해야 한다.** 거기서 *"휴지통으로 복구되므로 당분간 수용"*이라 했으나, 그 근거는 **웹이 덮어쓴 경우에만** 성립한다. SMB에서 in-place로 덮어쓴 것은 어느 쪽으로도 복구되지 않는다. 최소한 휴지통 디렉터리 레이아웃을 Samba `recycle`과 웹이 공유하도록 맞출 것.

### 🟡 검색 인덱스의 권한 필터링

[Phase 3](#phase-3--확장)의 검색 인덱싱은 [ADR-1](#adr-1-권한-모델은-posix-uidgid)과 정면으로 부딪힌다. 인덱스는 태생적으로 전역이고 권한의 진실은 파일시스템에만 있다. 결과 목록을 만들 때 **항목마다 해당 사용자 헬퍼로 접근 가능 여부를 재확인**하지 않으면 파일명·경로가 유출된다(파일명 자체가 기밀인 경우는 흔하다). 이 재확인은 비싸고, *"상위 20건 뽑고 필터링하니 3건 남음"* 같은 페이지네이션 문제도 따라온다.

### 🟡 기타

- **백업/DR 전략이 없다.** 휴지통은 백업이 아니다(랜섬웨어가 휴지통도 지운다). 초기 단일 노드 구간이 오히려 가장 위험하다 — 스냅샷 정책을 [Phase 2](#phase-2--운영-편의)에 넣었다
- 쿼터 정책 — CephFS는 유저 쿼터가 없고 **디렉터리 xattr 기반**이다. "사용자당 100GB"를 원하면 홈 디렉터리에 거는 방식이며, **디렉터리 레이아웃 확정 시점에 함께 결정**해야 한다. 휴지통을 쿼터에 포함할지도 같이 정할 것
- CephFS POSIX ACL은 커널 클라이언트 + `acl` 마운트 옵션에서만 정상 동작(ceph-fuse는 지원 약함)
- 검색 인덱싱 방식 — 별도 인덱서 vs 실시간
- 업로드 재개/청크 — [7장](#지금-피해둘-것-cephfs-호환성)은 "청크를 별도 파일로 쌓았다가 병합"이라 했는데 [6장](#6-파일-쓰기-규약)은 "임시 파일 하나, 예외 없음"이다. **서로 충돌한다** — 청크 병합 경로에도 최종 rename이 적용되도록 규약을 다시 쓸 것

---

## 부록: 참고 프로젝트

- **Filebrowser** (Go) — 웹 파일 매니저. 구조 참고용
- **Filestash** (Go) — 웹 서버가 localhost sshd에 해당 사용자로 SFTP 접속해 임퍼소네이션을 위임. 본 프로젝트에서는 SMB를 직접 제공하므로 SSH 경유는 불필요하나, "검증된 데몬에 임퍼소네이션을 떠넘긴다"는 발상은 ADR-3의 배경
- **Samba** — 헬퍼 프로세스 + fd 패싱 구조의 원형
- **usersync** (Go, 자체 개발) — 계정 생명주기 reconciler. [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)에서 채택. uid 재사용 방지(tombstone), Samba 공유 블록 생성, AD 인계 경로(`export --format csv|ldif` / `detach` / `mode: audit`)를 제공한다. 자격증명·AD 전환 계획은 그 저장소의 `identity-roadmap.md`
