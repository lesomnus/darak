# 사내 파일 서버 (가칭) — 설계 문서

> 상태: 초안 / 작성일 2026-08-08 / 개정 2026-08-07 (자격증명·계정관리 결정 반영)
>
> **이번 개정에서 확정된 것**
> - 자격증명은 **tdbsam 하나**. 웹은 `ntlm_auth`로 같은 것을 검증한다 → [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)
> - 웹 로그인에 한해 **SSO를 신원 주장으로만** 채택. 인가는 여전히 tdbsam, 매핑은 관리자 승인 → [ADR-2 개정](#개정-2026-08-13-sso를-신원-주장에-한해-채택한다)
> - 자동 프로비저닝은 **설정 파일로만** 선언하고, 새로 생긴 계정에만 결합한다 → [ADR-2 개정 2](#개정-2026-08-14-자동-프로비저닝--정책의-소재지가-옮겨간다)
> - 계정 생명주기는 **usersync**에 위임, uid/gid 대역 `3000–19999` → [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)
> - 초기 배치는 기존 서버(karina) 위, 이후 전용 장비로 이전
> - 온프렘 AD 도입 예정이나 **시기 미정** → [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)

---

## 1. 목적과 배경

사내 개인·팀 단위 파일 저장소를 구축한다. 상용 NAS 어플라이언스와 기존 오픈소스는 다음 이유로 부적합하다고 판단했다.

| 후보 | 부적합 사유 |
|---|---|
| Nextcloud | 무겁고, Unix 사용자와 매핑되지 않음. 자체 권한 모델이 파일시스템과 별개 |
| Seafile | 파일을 블록으로 쪼개 저장. POSIX 뷰가 없어 SMB·기존 도구와 병행 불가 |
| Filebrowser | 다중 사용자 권한 모델이 빈약 |
| 상용 NAS | 요구에 맞추기 어렵고 확장 경로가 벤더에 종속 |

따라서 **Go로 웹 UI + 관리 계층만 직접 구현**하고, 파일 접근 프로토콜과 권한 모델은 검증된 기존 구성요소(Samba, POSIX)에 위임한다.

### 설계 원칙

1. **진실의 원천은 파일시스템 하나.** 권한은 POSIX uid/gid/mode/ACL로만 표현한다. 애플리케이션 레벨 권한 모델을 별도로 두지 않는다.
2. **바퀴를 재발명하지 않는다.** SMB는 Samba가, 임퍼소네이션은 커널이, 원자성은 `rename(2)`가 담당한다.
3. **동시성보다 복구 가능성.** 락으로 충돌을 막는 대신, 덮어쓴 데이터를 항상 되살릴 수 있게 한다.

---

## 2. 요구사항

### 기능

- 웹 UI로 파일 브라우징 / 업로드 / 다운로드 / 공유 — **필수 경로.** 컴퓨터에 익숙하지 않은 사용자가 SMB 마운트 없이 사용
- SMB 마운트 — 익숙한 사용자를 위한 부가 경로. 개인 디렉터리 + 소속 팀 공유 디렉터리
- 개인 디렉터리(비공개) 및 팀 공유 디렉터리(그룹 단위 읽기/쓰기)
- 두 경로가 **동일한 데이터, 동일한 권한 규칙** 위에서 동작

### 비기능

- 초기: 스토리지 싱글 노드. 향후 CephFS로 무중단에 가깝게 전환 가능해야 함
- 워크로드는 **읽기 위주**. 동시 쓰기는 드물다고 전제
- 게이트웨이는 당분간 1대 운영
- **사내망/VPN 한정 운영. 인터넷에 직접 노출하지 않는다** — [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)가 MFA를 포기하는 전제이므로, 이 항목이 바뀌면 인증 결정을 다시 열어야 한다

### 명시적 비목표 (Non-goals)

- 동시 쓰기 조정(락, 임대, 충돌 병합) — [ADR-6](#adr-6-동시-쓰기--last-write-wins) 참조
- **셀프서비스 공유**(사용자가 다른 사용자에게 지속적 접근 권한 부여) — [10장](#-공유-링크--채택--셀프서비스-공유--비목표). 공유 **링크**는 별개이며 채택했다
- SMB/NFS 프로토콜 자체 구현
- 실시간 협업 편집
- **SSO가 인가를 답하는 것** — SSO는 웹 로그인에 한해 채택했으나 **누구인가만** 답한다. 계정 존재와 정지 여부는 여전히 tdbsam이 답한다 → [ADR-2 개정](#개정-2026-08-13-sso를-신원-주장에-한해-채택한다)
- **darak이 계정을 만드는 것** — 자동 프로비저닝을 켜도 계정을 만드는 것은 외부 엔드포인트와 usersync이고, darak은 요청하고 기다릴 뿐이다 → [ADR-2 개정 2](#개정-2026-08-14-자동-프로비저닝--정책의-소재지가-옮겨간다)
- **SMB의 MFA / SSO** — SMB에는 OIDC가 들어갈 자리가 없다. 사내망 전제 하에 의도적으로 포기하며, 답은 Kerberos뿐이고 그건 AD가 와야 온다 → [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)
- **디렉터리 서비스가 되는 것** — 계정은 [usersync](#adr-8-계정-생명주기는-usersync에-위임)가, 장차 AD가 관리한다
- **사용자별 설정의 서버 보관** — 테마·즐겨찾기·최근 폴더는 브라우저(localStorage)에 있다. 서버에 사용자 상태를 두기 시작하면 그것도 백업·마이그레이션·게이트웨이 다중화의 대상이 된다. 대가는 브라우저를 옮기면 따라오지 않는다는 것이고, 지금 그 목록의 가치에 비해 감당할 만하다

---

## 3. 아키텍처

```
[브라우저]                    [탐색기 / Finder]
    │ HTTPS                        │ SMB
    ▼                              ▼
┌──────────────────────────────────────────────────┐
│  컨테이너 (root)                                  │
│                                                  │
│  ┌───────────────┐   ┌────────┐   ┌───────────┐  │
│  │ Go 서버        │   │ Samba  │   │ usersync  │  │
│  │ 웹 UI · 관리   │   │ smbd   │   │ (부팅 시)  │  │
│  └───────┬───────┘   └────┬───┘   └─────┬─────┘  │
│          │ Unix socket    │             │        │
│          │ + SCM_RIGHTS   │             ▼        │
│          ▼                │        /etc/passwd   │
│  ┌───────────────┐        │        (파생 상태)    │
│  │ 유저별 헬퍼     │        │                     │
│  │ (uid 강등)     │        │                     │
│  └───────┬───────┘        │                      │
└──────────┼────────────────┼──────────────────────┘
           ▼                ▼
   ┌────────────────────────────────────┐
   │   POSIX 파일시스템 (볼륨)            │
   │   초기: 로컬 XFS  →  이후: CephFS   │
   └────────────────────────────────────┘

   유지되는 볼륨: 데이터 · /var/lib/samba(tdbsam) · 공유 링크
   유지되지 않음: /etc/passwd — 부팅마다 roster에서 재구성
```

### 컴포넌트 역할

**Go 서버** — 웹 UI, 인증, 공유 관리, 검색, 감사 로그, 썸네일. 파일 I/O는 헬퍼가 넘겨준 fd로 수행.

**헬퍼 프로세스** — 사용자별로 1개. 해당 사용자 권한으로 실행되며 경로를 열어 fd만 반환.

**Samba** — SMB 프로토콜과 그에 따른 임퍼소네이션 전담. Go 서버는 관여하지 않음. `tdbsam`이 **웹·SMB 공통 자격증명 저장소**이며, 웹은 `ntlm_auth`로 여기에 검증을 위임한다([ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)). **호스트가 아니라 컨테이너 안에서 돈다** — 이유는 [ADR-9](#adr-9-계정은-컨테이너-안에서-roster로부터-재구성한다).

**usersync** — 계정·그룹·tdbsam 항목의 생명주기. `roster.yaml`이 선언이고, **부팅 때마다 컨테이너 안에서 적용된다.** 파일서버는 계정을 만들지 않는다([ADR-8](#adr-8-계정-생명주기는-usersync에-위임), [ADR-9](#adr-9-계정은-컨테이너-안에서-roster로부터-재구성한다)).

---

## 4. 결정 사항 (ADR)

### ADR-1: 권한 모델은 POSIX uid/gid

**결정.** Unix 사용자/그룹/mode/ACL을 유일한 인가 체계로 사용한다.

**근거.** Samba는 SMB 인증 후 결국 Unix uid로 매핑해 파일에 접근한다. 즉 **SMB를 제공하기로 한 시점에 Unix 계정 체계는 이미 필수 요건이다.** 웹 UI만 별도 권한 모델을 쓰면 같은 데이터에 진실의 원천이 둘 생기고, 두 모델은 반드시 어긋난다.

**결과.** 셀프서비스 공유(임의의 사용자에게 임의 폴더 읽기 권한 부여)는 POSIX 그룹 폭발 문제를 낳는다. → [미결정 사항](#10-미결정-사항) 참조.

---

### ADR-2: 인증은 tdbsam 하나 (SSO 미채택)

**결정.** 인증(누구인가)은 **Samba의 `tdbsam` 하나**를 쓴다. 웹 서버는 `ntlm_auth`를 호출해 같은 저장소에 검증을 위임한다. 인가(무엇을 할 수 있나)는 파일시스템. **비밀번호는 사용자당 하나이고, 웹과 SMB가 그것을 공유한다.**

**근거.**

- **SMB는 어차피 SSO를 못 쓴다.** SMB2/3의 인증은 SPNEGO로 감싼 Kerberos 또는 NTLMSSP뿐이고 OIDC 토큰이 들어갈 자리가 없다. KDC가 없는 지금 SMB는 무조건 자기 자격증명을 쓴다. **SMB의 SSO는 Kerberos가 유일한 답이고, 그건 AD가 와야 생긴다.**
- **사내 IdP(Entra)가 전 직원을 커버하지 않는다.** 최저 등급이고 사실상 이메일 전용이라 계정이 없는 인원이 있다. 그들은 OIDC로 로그인할 수단 자체가 없다.
- **그래서 SSO는 도움이 가장 덜 필요한 층을 돕는다.** 매일 이메일을 쓰는 사람에겐 편하지만, [2장](#2-요구사항)이 명시적으로 우선한 "컴퓨터에 익숙하지 않은 사용자"는 대체로 그 반대편에 있다.
- **웹만 SSO를 붙이면 진실의 원천이 둘이 된다.** [설계 원칙 1](#설계-원칙)과 정면으로 어긋난다. 실무적으로는 오프보딩이 깨진다 — IdP만 잠그면 **웹은 막히는데 SMB 비밀번호는 살아 있는** 상태가 조용히 남는다. 단일 저장소면 `roster.yaml`의 `status: disabled` 한 줄로 둘 다 죽는다([ADR-8](#adr-8-계정-생명주기는-usersync에-위임)).
- PAM은 cgo 의존이 생겨 배포가 번거롭다. `ntlm_auth`는 exec 호출이라 정적 바이너리를 유지한다.

**대가: MFA가 없다.** 이 구성에서 실질적으로 포기하는 것은 이것 하나다. **수용 조건을 명시한다 — 서버를 인터넷에 직접 노출하지 않고 사내망/VPN 한정으로 운영한다.** 이 전제가 깨지면 결정을 다시 연다.

**결정을 뒤집는 조건.** ⓐ 외부 노출이 필요해짐, ⓑ 온프렘 AD 도입으로 전 직원이 도메인 계정을 갖게 됨, ⓒ Entra 커버리지가 전원으로 확대됨. 그래서 **웹 인증은 `Authenticator` 인터페이스 뒤에 두고 구현은 `ntlm_auth` 하나만** 둔다.

**AD가 오면 공짜로 승계된다.** 도메인 조인 후 같은 `ntlm_auth` 호출이 winbind를 경유해 AD를 인증한다 — **웹 인증 코드는 한 줄도 안 바뀐다.** 이것이 이 선택의 실질적 이득이다.

**운영 전제.** `ntlm_auth`는 winbindd를 경유한다. standalone Samba에서는 winbindd를 안 띄우는 경우가 많으므로 **웹 인증을 붙이기 전에 실행 상태로 만들어야 한다.** 어차피 AD 조인 후엔 필수이므로 지금 켜 두면 전환 시 바뀔 것이 하나 줄어든다. 정확한 호출 형태와 권한 요건(root 또는 `winbindd_priv` 그룹)은 배포 대상 Samba 버전에서 실측할 것.

#### 개정 (2026-08-13): SSO를 **신원 주장에 한해** 채택한다

위 결정 중 뒤집힌 것은 하나도 없다. 뒤집힌 것은 **"SSO를 붙이면 자격증명 저장소가 둘이 된다"는 전제**다. 그 전제는 SSO가 *인가*까지 답한다고 가정했을 때만 참이고, 인가를 넘기지 않으면 성립하지 않는다.

**결정.** IdP는 **"이 브라우저 앞의 사람이 누구인가"만** 답한다. 그 답이 어느 계정인지는 서버가 가진 매핑이 정하고, **그 계정이 지금 로그인해도 되는가는 여전히 tdbsam에 묻는다** — 비밀번호 경로가 묻는 바로 그 저장소에, 매 로그인마다. 구현은 `internal/auth`의 `Gate`이며 NSS 해석과 `pdbedit`의 `D` 플래그를 본다.

따라서 원래의 반론은 그대로 유효한 채 해소된다:

- **오프보딩은 여전히 한 곳이다.** `roster.yaml`의 `status: disabled` → `usersync apply` → tdbsam `D` → SMB·비밀번호 로그인·SSO가 **동시에** 닫힌다. IdP를 잠그지 않아도 닫히고, IdP만 잠가서 절반만 닫히는 상태는 만들 수 없다.
- **IdP 커버리지가 전 직원이 아니어도 된다.** SSO는 추가 경로이지 대체 경로가 아니다. 계정이 없는 사람은 지금까지처럼 아이디와 비밀번호로 들어온다. 로그인 화면은 두 경로를 동등하게 보여준다.
- **SMB는 변하지 않는다.** OIDC 토큰이 들어갈 자리는 여전히 없고, SMB의 SSO는 Kerberos뿐이며 그건 AD가 와야 생긴다.

**따라서 "인터넷 미노출" 전제는 그대로다.** 웹 경로는 IdP의 MFA를 얻지만 SMB 경로는 여전히 비밀번호 하나이므로, 외부 노출은 이 개정이 허가하지 않는 별개의 결정이다.

**매핑은 roster에 두지 않는다.** roster가 git에 있는 이유는 uid가 디스크의 바이트에 용접돼 있기 때문이다 — ZFS 스냅샷은 불변이라 한 번 틀린 uid는 영구히 틀리고, 재사용되면 남의 파일을 상속한다. 이메일에는 그 성질이 없다. 고치는 데 커밋이 필요 없고, 디스크에 아무 흔적도 남기지 않는다. 이것은 **SMB 비밀번호와 같은 부류**이며, 그것이 이미 원장 밖에 있는 이유와 같은 이유로 밖에 둔다([ADR-8](#adr-8-계정-생명주기는-usersync에-위임), `internal/admin/ops.go`가 그 경계를 적어 둔 곳). 저장소는 `-identities` 플래그가 가리키는 JSON 파일이고, 공유 링크 저장소와 같은 이유로 데이터 볼륨 밖에 있다([7장](#7-볼륨-레이아웃)).

**대신 잃는 것과 그 보상.** 매핑을 git에서 빼면 `git blame`이 답하던 "이 주소를 누가 언제 붙였나"가 사라진다. 그 줄 하나하나가 "누가 누구로 로그인해도 되는가"에 대한 진술이므로 기록은 어딘가 있어야 한다 — `-identity-journal`의 append-only JSONL이 그것이다.

**등록은 자동이 아니다.** 매핑되지 않은 신원이 SSO로 들어오면 **요청만 기록되고 로그인은 거부된다.** 관리자가 관리 페이지에서 계정을 지정해야 로그인이 가능해진다. 이 순서인 이유는 오타 때문이다 — 관리자가 주소를 미리 받아쓰면 IdP가 실제로 주장하는 값(어느 클레임인지, 대소문자, 여러 주소 중 무엇인지)과 어긋나기 쉽고, 어긋나면 **비밀번호를 틀린 것과 구별되지 않는 401**이 된다. 사람이 한 번 로그인하고 그 신원을 그대로 포착하면 그 실패 모드 자체가 없어진다.

**첫 로그인에 `sub`를 고정한다(TOFU).** 승인 후 첫 로그인에서 IdP의 불변 식별자를 기록하고, 그 뒤로는 **주소가 아니라 그 식별자가 인증한다.** 주소만으로는 막을 수 없는 사고가 하나 있기 때문이다 — 퇴사자의 주소가 1년 뒤 신입에게 재할당되는 경우. 고정된 `sub`와 다른 `sub`가 같은 주소를 들고 오면 거부되고 기록된다. 이것은 런타임에 발견되는 값이라 애초에 GitOps로는 표현할 수 없고, 저장소를 앱이 소유하기로 한 결정이 가능하게 한 것이다.

**IdP 쪽 필수 검증.** 서명·`iss`·`aud`·`exp`(라이브러리), `nonce`(브라우저 결속), 그리고 **`tid`(테넌트)**. 마지막 것이 없으면 다른 테넌트나 개인 계정이 원하는 주소를 주장하는 토큰이 정상 검증된다 — 주소를 식별자로 쓰는 것 자체가 무의미해진다. 그래서 `/common`·`/organizations`·`/consumers` 발급자는 테넌트를 명시하지 않으면 기동을 거부한다. 단일 테넌트 발급자 URL은 `iss` 검사가 이미 테넌트를 고정하므로 필요 없다.

**AD가 오면.** 이메일→계정 매핑은 디렉터리가 답하게 되므로 이 파일은 **지우면 된다.** roster 스키마에 넣지 않은 실질적 이유가 이것이다.

#### 개정 (2026-08-14): 자동 프로비저닝 — 정책의 소재지가 옮겨간다

**결정.** 매핑 없는 신원이 들어왔을 때, 설정 파일의 규칙에 맞으면 외부 엔드포인트에 계정 생성을 요청하고, 계정이 실제로 나타나면 관리자 승인 없이 로그인시킨다. 기본은 꺼짐이고, 켜지 않으면 위의 승인 흐름 그대로다.

**이것이 정직하게 바꾸는 것.** 지금까지 이 시스템에는 **사람의 행위 없이 접근이 생기는 경로가 없었다.** 이 결정은 그것을 만든다. 게이트가 "관리자가 승인 버튼을 누른다"에서 "엔드포인트가 구현한 정책"으로 옮겨가고, **그 정책은 이 저장소 밖에 있다.** 테넌트가 곧 재직자 명단인 조직에서는 옳은 선택이지만, 소재지가 바뀐다는 사실 자체를 기록해 둔다.

**그 이동에 대한 대가로 네 가지를 건다.**

1. **규칙은 파일이지 웹 페이지가 아니다.** 관리 페이지에서 엔드포인트를 바꿀 수 있으면 그 페이지가 곧 자기 자신에게 계정을 발급하는 경로다. 쓰기 라우트는 존재하지 않고, 페이지는 **적용 중인 내용을 보여주기만** 한다(경로·반영 시각·내용 해시·규칙).
2. **darak은 계정을 만들지 않는다.** 요청할 뿐이고, 계정이 NSS와 tdbsam에 나타나는지 — 즉 모든 로그인이 통과하는 그 게이트를 — 기다린다. roster와 usersync가 여전히 계정의 주인이므로 비목표 "디렉터리 서비스가 되는 것"은 깨지지 않는다.
3. **이미 존재하던 계정에는 결코 자동 결합하지 않는다.** 엔드포인트가 응답한 시점에 이미 해석되는 이름은 방금 만들어진 것이 아니므로 승인 큐로 간다. 이 한 줄이 "엔드포인트가 아무나 지목할 수 있다"를 **"방금 자기가 만든 것만 건넬 수 있다"**로 바꾼다 — 망가진 엔드포인트의 피해가 쓰레기 계정 생성에 머물고, 남의 홈 디렉터리 인계가 되지 않는다.
4. **신원당 1회 발사, 전역 상한, 열리는 쪽으로 실패하지 않음.** 발사 가능한 모집단이 테넌트 전원이기 때문이다.

**그리고 로컬 사전 필터.** 엔드포인트의 정책을 신뢰하기 전에 도메인과 클레임으로 거른다. 규칙 밖의 사람은 지금처럼 사람이 승인한다 — **자동 경로는 추가지 대체가 아니다.**

**대기는 최적화이지 요구사항이 아니다.** 계정 반영이 늦어 대기가 끝나도 실패로 처리하지 않는다. 다음 로그인이 완료시킨다. 이 성질 때문에 대기 시간 값은 튜닝할 가치가 없다.

**설정은 재시작 없이 다시 읽힌다.** 잘못된 편집은 운영 페이지에 보고되고 **직전 규칙이 그대로 유지된다** — [ADR-9](#adr-9-계정은-컨테이너-안에서-roster로부터-재구성한다)의 roster hot-reload가 따르는 규칙과 같다. 오타가 장애가 되어서는 안 되고, 오타 때문에 규칙이 조용히 사라져서도 안 된다.

---

### ADR-3: 임퍼소네이션은 헬퍼 프로세스 + fd 패싱

**결정.** 사용자별 헬퍼 프로세스를 `setpriv --reuid --regid --init-groups`로 강등해 띄우고, 유닉스 소켓으로 open 요청을 받아 `SCM_RIGHTS`로 fd를 전달한다.

**검토한 대안과 탈락 사유:**

| 대안 | 탈락 사유 |
|---|---|
| `syscall.Setuid` | Go 런타임이 모든 스레드에 브로드캐스트. goroutine별 분리 불가 |
| `setfsuid` + `LockOSThread` | 동작은 하나 원복 누락 시 위험. 보조 그룹 처리 등 미묘한 부분 많음 |
| root로 접근 + 앱이 권한 검사 | mode bits·보조 그룹·ACL·sticky bit를 재구현해야 함. 틀리면 곧 보안 사고 |
| 요청마다 `su -c` | PAM 세션 스택 전체 실행(wtmp, logind scope 생성). 무겁고 로그 오염. 셸 경유로 인젝션 위험 |
| 요청마다 `setpriv` fork | fork/exec 비용이 요청마다 발생. 파이프 경유라 Range·seek 곤란 |
| 권한 "확인"만 자식으로, 읽기는 root로 | **TOCTOU.** 검사와 접근이 같은 권한 컨텍스트에서 원자적으로 일어나야 함 |

**이점.** fd에 권한 검사 결과가 박혀 있어 TOCTOU가 없다. `io.Copy`로 sendfile 경로를 탈 수 있다. Samba가 20년간 검증한 구조다. 유휴 헬퍼는 타임아웃으로 회수.

#### ⚠️ 헬퍼 프로토콜의 범위 — 이 설계 최대의 함정

**fd 패싱이 커버하는 것은 "그 파일의 read/write"뿐이다.** `rename`·`unlink`·`mkdir`·`link`·`O_CREAT`는 **디렉터리 쓰기 권한을 호출 프로세스의 자격증명으로 매번 새로 검사**한다. 헬퍼가 넘겨준 dirfd로 `renameat(dirfd, …)`을 해도 마찬가지고, **호출자가 root면 DAC를 전부 통과한다.**

즉 헬퍼 역할을 "경로를 열어 fd만 반환"으로 두면, [6장 쓰기 규약](#6-파일-쓰기-규약)의 4단계 중 Go 서버가 직접 해도 되는 것은 **내용 write 하나뿐**이다. 나머지를 root 서버가 수행하면 권한 검사가 통째로 사라지고, 위 표에서 탈락시킨 *"root로 접근 + 앱이 권한 검사"* 로 그대로 회귀한다.

**따라서:**

1. 헬퍼 프로토콜은 `OPEN`이 아니라 **오퍼레이션 집합**이다 — `OPEN` / `OPENAT_CREATE` / `MKDIR` / `RENAME` / `LINK` / `UNLINK` / `RMDIR` / `STAT` / `CHMOD` / `SETXATTR` / `READDIR`. Phase 0의 "프로토콜 설계"는 여기서 말하는 것보다 훨씬 큰 작업이다.
2. **불변식: Go 서버는 경로 기반 syscall과 `*at()` 계열을 절대 직접 호출하지 않는다.** 서버가 만지는 것은 헬퍼가 넘겨준 fd에 대한 `read`/`write`/`pread`/`sendfile`/`getdents`뿐. 코드 리뷰 체크리스트나 lint 규칙으로 강제할 가치가 있다.
3. **[ADR-4](#adr-4-경로-안전성은-커널에-위임)의 `openat2` 래퍼는 서버가 아니라 헬퍼 안에 있어야 한다.** 경로 해석 자체가 사용자 자격증명으로 일어나야 한다.

#### 헬퍼 수명 주기 — 아직 비어 있는 부분

- **요청 다중화**가 없다. 소켓 위 단순 req/resp면 head-of-line blocking이 생긴다(큰 업로드 하나가 그 사용자의 디렉터리 목록 조회를 막음). 요청 ID 기반 멀티플렉싱이나 소규모 풀이 필요하다.
- `setpriv --init-groups`는 **기동 시점의 보조 그룹을 고정**한다. 팀에 새로 추가된 사용자는 헬퍼가 회수될 때까지 접근이 안 된다. 그룹 변경 시 해당 헬퍼를 강제 재기동하는 경로가 필요하다.
- 사용자 수 × 헬퍼 = 프로세스/fd 리소스. 상한과 LRU 회수 정책을 정할 것.
- 소켓은 root-only 디렉터리에 두고 `SO_PEERCRED`로 검증.

#### 서버가 root일 필요가 있는가

서버에 root가 필요한 이유는 **헬퍼를 `setpriv`로 띄우기 위해서** 하나뿐이다. 그런데 서버는 HTTP 파싱·썸네일 생성(libvips/ImageMagick — 전통적인 RCE 벡터)·검색 인덱싱까지 떠안는 **이 시스템 최대의 공격 표면**이다.

→ 아주 작은 root 전용 **spawner 데몬**("이 uid로 헬퍼 하나 띄워서 소켓 fd를 돌려줘"만 하는 수백 줄)을 두고 Go 서버는 비특권으로 실행하는 구조를 검토할 것. 그러면 [ADR-4](#adr-4-경로-안전성은-커널에-위임)의 "root라서 path traversal 하나가 전체 유출" 전제가 약해진다(그래도 `openat2`는 유지). 썸네일 생성은 별도 seccomp 샌드박스 워커로 빼는 것을 강력히 권한다.

---

### ADR-4: 경로 안전성은 커널에 위임

**결정.** 문자열 기반 경로 검증을 하지 않는다. `openat2(2)`에 `RESOLVE_BENEATH | RESOLVE_NO_MAGICLINKS`를 지정한다. Go 1.24의 `os.Root` / `os.OpenInRoot` 또는 `golang.org/x/sys/unix.Openat2` 사용.

**근거.** 서버가 root로 동작하므로 path traversal 한 건이 곧 전체 파일시스템 유출이다. 심볼릭 링크·`..`·경합 조건을 애플리케이션이 전부 막는 것은 비현실적이다.

---

### ADR-5: SMB는 Samba에 위임, uid 매핑은 문제가 아님

**결정.** SMB 프로토콜을 구현하지 않는다. 호스트에 Samba를 띄운다.

**정리해둘 사실.** SMB에는 uid가 흐르지 않는다. 세션 기반 인증 프로토콜이며, 서버가 인증된 사용자로 파일 작업을 수행하고 결과만 반환한다. 클라이언트는 서버 측 uid를 알지도, 보내지도 않는다.

- Windows / macOS: uid 개념 자체가 없음
- Linux(cifs): 마운트 옵션 `uid=`/`gid=`로 지정한 값으로 **표시만** 됨. 로컬 눈속임이며 실제 권한과 무관. 필요 시 `noperm`으로 클라이언트 측 검사 비활성화

즉 서버에서 개인 디렉터리 소유자가 1047번이든 5000번이든 클라이언트 경험에 영향이 없다. **권한 검사는 100% 서버에서 일어난다.**

---

### ADR-6: 동시 쓰기 — Last Write Wins

**결정.** 락 매니저를 구현하지 않는다. lost update를 허용한다.

**근거.** 워크로드가 읽기 위주다. 락 소유자 크래시 복구, 임대 갱신, 클러스터 전역 락 상태 관리 등 복잡도 덩어리 전체를 제거할 수 있다.

**단, 아래 두 가지는 포기하지 않는다.** LWW의 실제 위험은 충돌 자체가 아니라 **손실이 조용하다는 것**이다.

1. **원자적 교체** — torn file 방지. 원자성 문제이지 동시성 문제가 아니며 비용이 사실상 0
2. **휴지통 경유 덮어쓰기** — 날아간 작업을 항상 복구 가능하게

→ [파일 쓰기 규약](#6-파일-쓰기-규약) 참조.

> ⚠️ **2번은 한동안 성립하지 않았다.** 휴지통 이름이 1초 해상도인데 삭제 경로가 평범한 `rename`을 썼고, `rename`은 목적지를 **말없이 대체**한다. 같은 basename을 1초 안에 두 번 지우면 첫 판본이 사라지고 API는 두 번 다 204를 돌려줬다. 덮어쓰기 경로는 처음부터 충돌 시 재시도하고 있었고 삭제 경로만 빠져 있었다 — `rename`이 충돌을 **보고하지 않기** 때문에 같은 실수가 눈에 띄지 않았다.
>
> LWW를 수용 가능하게 만드는 근거 전체가 "이전 판본은 휴지통에 있다"이므로, 이 구멍은 ADR-6 자체를 무너뜨린다. `RENAME_NOREPLACE` + 접미사 재시도로 막았고 회귀 테스트로 고정했다([§6 휴지통 위치](#휴지통-위치)).

**알려진 구멍.** SMB 세션의 share mode를 Go 서버가 인지하지 못한다. 누군가 엑셀로 열어둔 파일을 웹에서 덮어쓸 수 있다. `kernel share modes = yes` + `flock(LOCK_EX|LOCK_NB)`으로 감지 가능하나 성능 비용이 있고 CephFS 동작 확인이 필요하다. 발생 빈도가 낮고 휴지통으로 복구되므로 **당분간 수용**한다.

> ⚠️ **위 "휴지통으로 복구되므로" 근거는 절반만 맞다.** 웹이 덮어쓴 경우에만 성립하고, **SMB에서 in-place로 덮어쓴 것은 어느 쪽으로도 복구되지 않는다.** LWW를 수용 가능하게 만드는 안전망이 한쪽 경로에만 있다는 뜻이다 → [10장](#-데이터-보호의-경로-비대칭--수용).

---

### ADR-7: 스토리지는 CephFS 전환을 전제로 설계

**결정.** 초기에는 로컬 파일시스템, 이후 CephFS로 전환. Go 서버 코드는 POSIX 경로만 다루므로 변경 없음.

**CephFS의 uid 모델 (오해 정리).** CephFS는 로컬 파일시스템과 동일하게 inode에 uid/gid를 **정수로** 저장한다. Ceph 내부에 사용자 DB는 없다. 신뢰 모델은 NFSv3과 같아서, 마운트가 허용된 머신의 root는 사실상 임의 uid를 사칭할 수 있다.

요구사항은 하나뿐이다: **마운트하는 모든 노드가 uid 번호를 동일하게 해석할 것.**

그런데 **직원 단말은 CephFS를 마운트하지 않는다.** 웹 또는 SMB로 들어온다. CephFS를 마운트하는 건 관리 대상 게이트웨이 서버 몇 대뿐이므로, uid 일치 범위가 회사 전체가 아니라 서버 2~3대다.

**그 일치를 보장하는 것이 [usersync](#adr-8-계정-생명주기는-usersync에-위임)다.** `roster.yaml`이 번호의 정본이므로 게이트웨이마다 같은 roster로 `apply`하면 uid가 일치한다. 온프렘 AD 도입 후에는 NSS가 그 역할을 넘겨받고, usersync는 `mode: audit`으로 **번호가 여전히 일치하는지 감시하는** 쪽으로 물러난다.

필요 시 MDS caps로 경로를 제한할 수 있다. **경로는 CephFS 네임스페이스 안의 경로**이지 호스트 마운트 지점이 아니다 — `/srv/data`는 우리가 붙이는 자리이므로 여기 쓰면 존재하지 않는 서브트리를 허용하게 된다:
```
ceph fs authorize cephfs client.gw / rw          # 파일시스템 루트 전체
# 또는 CephFS 안에 /data 서브트리를 두고 그것만:
ceph fs authorize cephfs client.gw /data rw
```

**단, uid를 고정하는 caps는 이 게이트웨이에 쓸 수 없다.**
```
caps mds = "allow rw path=/home uid=1000 gids=1000,1001"   # ← 게이트웨이에는 부적합
```
이 형태는 **매칭 필터**다 — 클라이언트가 보낸 `caller_uid`/`caller_gid`가 cap에 적힌 값과 다르면 어떤 grant도 매칭되지 않아 MDS가 권한 오류를 돌려준다. 소유자를 1000번으로 바꿔 쓰는(rewrite/squash) 것이 아니라 **그 외의 uid로 하는 연산을 전부 거부**하는 것이다.

결과적으로 게이트웨이에는 쓸 수 없다는 결론은 같다. 게이트웨이는 정의상 여러 uid를 번갈아 사칭해야 하므로(그게 [ADR-3](#adr-3-임퍼소네이션은-헬퍼-프로세스--fd-패싱)의 전부다), 이 caps를 걸면 지정한 한 사람 외의 모든 요청이 실패한다. 게이트웨이에는 **경로 제한만** 걸고 uid 제한은 걸지 않는다.

**`root_squash`도 현 구조와 충돌한다.** Go 서버가 root로 돌면서 팀 디렉터리 프로비저닝(`chown`/`chgrp`)을 하는 한 그 작업이 막힌다. 다만 [ADR-3의 spawner 분리](#서버가-root일-필요가-있는가)를 채택해 서버를 비특권화하면 오히려 켤 수 있게 된다 — 파일 I/O는 전부 비-root 헬퍼가 하기 때문이다. 두 결정은 연동되어 있다.

---

### ADR-8: 계정 생명주기는 usersync에 위임

**결정.** Unix 계정·그룹·tdbsam 항목의 생성/비활성/은퇴는 [**usersync**](https://github.com/lesomnus/usersync)가 관리한다. `roster.yaml`이 선언이고 `usersync apply`가 수렴시킨다. 파일서버는 계정을 직접 만들지 않는다.

**roster 의 모양** — 스키마 정본은 usersync 레포의 [README](https://github.com/lesomnus/usersync) + [`proto/usersync/roster.proto`](https://github.com/lesomnus/usersync/blob/main/proto/usersync/roster.proto)다:

```yaml
groups:
  - { name: perception, gid: 10001, description: Perception team }   # 그룹은 {name, gid, [description]}
users:
  - { name: alice, uid: 3001, groups: [perception] }                # 멤버십은 user.groups 로
  - { name: bob,   uid: 3005, status: reserved }                    # uid 소각(재사용 금지)
```

> ⚠️ **멤버십은 group 이 아니라 user 에 있다.** roster 의 `Group` 에는 `members` 필드가 없어 strict decode 가 거부한다. 이 레포 `internal/admin` 의 `Group{name, gid, members}` 는 **런타임 admin API 모델**(팀 패널이 렌더링하는 *해석된* 뷰)이라 roster 입력 스키마와 다르다 — 둘을 혼동하면 `unknown field "members"` 로 apply 가 깨진다.

**근거.** [ADR-1](#adr-1-권한-모델은-posix-uidgid)이 POSIX를 유일한 인가 체계로 정한 순간, 이 시스템에는 "누가 uid를 발급하고 회수하는가"라는 장부가 필요해진다. usersync는 이미 그것이고, 특히 **uid 재사용 방지**를 정면으로 다룬다 — 은퇴자를 지우는 대신 `status: reserved` tombstone으로 남겨 번호를 영구 점유한다. 이 문제는 자체 구현했다면 반드시 나중에 사고로 배웠을 것이다.

**ID 대역.** `usersync.yaml`의 `manage` 창이 정본이다.

| 대역 | 용도 |
|---|---|
| `3000`–`9999` | 사용자 uid. UPG의 gid가 uid와 같으므로 이 번호대는 gid로도 소비된다 |
| `10000`–`19999` | 팀 그룹 gid |

**→ 온프렘 AD 도입 시 IT에 `3000–19999` 대역 예약을 문서로 요청해야 한다.** 예약은 일회성 협상이고 번호는 공짜인 반면, **재번호는 사실상 불가능**하다 — 라이브 파일은 `chown -R`로 고쳐도 스냅샷은 불변이라 못 고치고, 과거 스냅샷이 영구히 틀린 uid로 남는다. 그래서 현 인원보다 넉넉하게 잡았다.

**계정명의 모양.** `^[a-z_][a-z0-9_.-]{0,31}$` — usersync가 정본이고 파일서버가 같은 것을 복사해 갖는다(둘이 어긋나면 계정이 존재하면서 웹 로그인만 안 되고, 그 실패는 비밀번호 오류와 구별되지 않는다).

**점이 들어간다**는 것이 유일하게 논쟁적인 부분이다. 한때 "smb.conf 지시어 주입"을 근거로 배제했는데 그 근거는 점에 해당하지 않는다 — 점은 `[...]` 섹션을 닫지 못하고, 닫을 수 있는 것은 개행뿐이며 그건 `groupadd`가 먼저 거부한다. 실제로 확인한 뒤 풀었다. **첫 글자 제한이 실제 방어**다: 앞의 `-`(`useradd`가 플래그로 읽는다)와 `.`·`..`를 막는다.

허용해야 하는 이유는 위 AD 전환이다. 디렉터리 서비스가 넘겨주는 이름은 `firstname.lastname` 형태일 가능성이 높고, **그때 이름 규칙을 바꾸는 것은 uid가 이미 파일에 박힌 뒤**라 훨씬 비싸다.

**AD 전환.** 온프렘 AD 도입 예정이나 **시기 미정**. 전환은 "uid 번호를 누가 답하는가"가 `/etc/passwd`에서 AD로 넘어가는 것 하나이고, `idmap_ad` + RFC2307로 **같은 번호를 그대로 심으면 chown·데이터 이동 0회**다. usersync가 이를 위해 `export --format csv|ldif`(번호 주입용 출력), `detach`(로컬 계정만 놓아주고 홈은 보존), `mode: audit` + `audit`(인계 후 장부 감시)를 제공한다. 상세는 usersync 저장소의 `identity-roadmap.md`.

> ⚠️ **Samba는 Entra ID(클라우드)에 도메인 조인할 수 없다.** `net ads join`이 되는 대상은 진짜 AD DS뿐이다. 도입 대상이 온프렘 AD DS인지 확인이 필요하며, 아니라면 Kerberos SMB SSO는 영원히 오지 않고 [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택)의 tdbsam 구조가 영구가 된다.

**결과.** 아래 5장의 디렉터리 레이아웃·Samba 공유 블록은 **usersync가 생성한다**(`usersync shares --write`). 이 문서의 예시는 그 출력과 일치해야 하며, 어긋나면 usersync 쪽이 정본이다.

---

### ADR-9: 계정은 컨테이너 안에서 roster로부터 재구성한다

**결정.** Go 서버·Samba·usersync를 **한 컨테이너**에 넣는다. `/etc/passwd`는 유지하지 않고 **부팅 때마다 `roster.yaml`에서 다시 만든다.** 유지하는 것은 데이터, `/var/lib/samba`(tdbsam), 공유 링크뿐이다.

**근거.**

- **계정이 귀한 상태가 아니라 파생 상태가 된다.** 백업할 것도, 드리프트도, *"누가 손으로 `useradd` 했나"* 도 없다. roster 하나가 진실이고 나머지는 언제든 다시 만들어진다 — [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)이 주장하던 것이 강제되는 형태가 된다.
- **되기 때문이다.** 볼륨 안의 데이터는 소유자를 **번호로** 안다. `/etc/passwd`는 그 번호에 이름을 붙이는 기록일 뿐이므로, roster가 번호를 고정하는 한 재구성해도 파일은 계속 같은 사람의 것이다. 이건 [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)의 AD 전환이 기대는 것과 **정확히 같은 성질**이고, 이 배포는 그걸 켤 때마다 한 번씩 실증한다.
- **게이트웨이 다중화가 쉬워진다.** 이 문서의 [ADR-7](#adr-7-스토리지는-cephfs-전환을-전제로-설계)은 원래 CephFS로 게이트웨이가 여러 대가 될 때의 uid 일치를 *"Ansible로 `/etc/passwd` 배포"* 로 답하고 있었다. 노드마다 같은 roster로 재구성하면 그 배포 단계 자체가 없어진다.

**Samba가 컨테이너 안이어야 하는 이유.** 호스트에 두면 호스트에도 같은 계정이 있어야 하고, 그러면 같은 roster를 두 곳에 적용하게 된다 — 사본이 둘이면 맞추는 일이 생긴다. 둘을 한 네임스페이스에 두면 계정 DB가 하나다.

**"안전"이라는 말은 조심해서 쓴다.** 컨테이너는 root로 도는 프로세스에 대해 강한 경계가 아니고, 이 컨테이너는 어차피 데이터 볼륨 전체를 쥔다. 실질 이득은 격리가 아니라 **재현성과 단일 진실**이다. 호스트 계정 DB를 건드리지 않는 건 부수적 이점이다.

**전제와 함정.**

| | |
|---|---|
| **userns-remap** | 켜져 있으면 컨테이너 uid 3001이 디스크에서는 다른 번호가 되어 **모든 파일의 소유권이 조용히 어긋난다.** 아무것도 그 시점에 실패하지 않고, 몇 달 뒤 "내 파일이 안 열린다"로 나타난다. → 서버가 기동 시 `/proc/self/uid_map`을 확인하고 **거부**한다(`-allow-remapped-uids`로만 무시 가능) |
| `tdbsam` | 비밀번호는 roster에서 재생성할 수 없다. `/var/lib/samba`는 반드시 볼륨 |
| `secrets.tdb` | AD 조인 후 머신 계정 비밀이 여기 들어간다. 같은 볼륨. 없으면 재시작마다 도메인에서 떨어진다 |
| 호스트 도구 | 백업·`ls -l`이 이름 대신 숫자를 본다. `rsync --numeric-ids`면 되지만 사람이 볼 땐 불편하다 |
| 445 포트 | 호스트 네트워킹을 쓴다. Windows·macOS 클라이언트가 445 자체를 원하고, 클라이언트와 smbd 사이의 NAT는 디버깅하고 싶지 않은 문제다 |

**구성.** `deploy/prod/`. 로컬 테스트용은 `deploy/local/`이며 같은 구조를 쓴다 — 차이는 TLS, 비밀번호, 그리고 계정 관리가 usersync인지 대역 스크립트인지뿐이다.

---

### ADR-10: 운영 권한은 POSIX 그룹으로, 팀 관리는 팀 소유자에게 위임

**결정.** 권한을 **두 축**으로 나눈다. 둘 다 선언에서 나오고, 둘 다 요청마다 다시 판정한다.

| 축 | 출처 | 할 수 있는 것 |
|---|---|---|
| **관리자** | `admin` POSIX 그룹 멤버십 | 운영 페이지 전부 — 조회, SMB 잠금/해제, 비밀번호 재설정, 모든 팀의 구성원 변경 |
| **팀 소유자** | roster의 `groups[].owners` | **자기 팀의 구성원 추가·제외, 그것뿐** |

- **읽기**(관리자): 사용자·팀 목록(uid/gid/홈/소속), SMB 계정 상태, 디스크 용량·여유, 사용자별 사용량, roster ↔ 실제 시스템 드리프트(`usersync audit`).
- **쓰기**(관리자): SMB 계정 잠금/해제, SMB 비밀번호 재설정. 둘 다 tdbsam만 바뀐다.
- **쓰기**(관리자 또는 소유자): 팀 구성원 — `users[].groups`만, `usersync member` 경유.
- **없음**: 계정 생성·삭제, uid 변경, `status` 변경, 소유자 지정.

**소유자는 작은 관리자가 아니다.** 관리자는 계정에 대해 거의 모든 것을 할 수 있고, 소유자는 **한 가지만** 할 수 있다. 그래서 관리자라고 해서 자동으로 소유자가 되지도 않는다 — 어떤 팀을 실제로 운영하는 관리자는 다른 사람과 똑같이 `owners`에 적힌다. 그렇지 않으면 *"이 팀은 누가 책임지는가"* 에 답할 수 없어진다.

**왜 권한을 앱이 아니라 커널의 그룹으로 정하나.** 이 서버에서 *"할 수 있는가"* 를 정하는 것은 처음부터 POSIX였다([ADR-1](#adr-1-권한-모델은-posix-uidgid)). 관리자 판정만 애플리케이션 테이블에 두면 진실이 둘이 되고, 둘은 어긋난다. 같은 그룹을 쓰면 `gpasswd -d`로 뺀 사람이 **즉시** 페이지를 잃는다 — 세션은 12시간짜리라, 로그인 시점에 관리자 여부를 도장 찍어 두면 권한을 뺏은 뒤에도 하루가 남는다. 그래서 판정은 요청마다 다시 한다.

**그룹은 roster의 팀이 아니다.** roster에 그룹을 하나 선언하면 팀 폴더와 SMB 공유가 따라온다([§5](#5-권한-레이아웃)). 계정을 관리할 수 있다는 것이 공유 디렉터리를 만들 이유는 아니므로, `admin`은 엔트리포인트가 **gid 2000**(관리 대역 10000–19999 아래, 시스템 바닥 1000 위)에 만든다. usersync는 이 번호를 만들지도, `audit`에서 문제 삼지도 않는다.

**소유자는 왜 POSIX에도 있나.** `owners`는 usersync가 `/etc/gshadow`의 관리자 필드(`gpasswd -A`)에 적용한다. POSIX에 이미 있는 개념이라 발명이 아니고, 적용하기 때문에 `usersync plan`이 어긋남을 잡는다. 다만 **인가는 gshadow가 아니라 roster에서 읽는다** — 보통 둘은 일치하지만 "보통"은 인가 판정의 근거가 못 되고, gshadow를 쓸 수 있는 사람이 스스로에게 위임을 부여할 수 있게 된다.

**왜 계정 생성은 없고 팀 배정은 있나.** 두 가지가 다르기 때문이다.

`roster.yaml`은 **누구에게 어떤 uid를 줬는지의 원장**이고, 그 파일의 git 이력이 곧 계정 관리 이력이다([ADR-8](#adr-8-계정-생명주기는-usersync에-위임)). 웹에서 사용자를 만들면 리뷰 없이 번호가 발급된다 — uid 재사용 사고를 막는 장치가 전부 그 원장에 걸려 있는데 말이다. 그래서 계정 생성·uid·`status`는 여전히 손편집이고, `owners` 지정도 마찬가지다(위임을 주는 것 자체가 위임될 수는 없다).

팀 배정은 그렇지 않다. uid를 발급하지도, 예약을 풀지도 않고, 프로젝트가 시작하고 끝날 때마다 바뀐다 — **변화율이 다르다.** 커밋과 재시작을 거치게 하면 실제로는 아무도 하지 않게 되고, 그러면 팀 폴더 권한이 현실과 어긋난 채로 남는다. 그래서 이것만 위임한다.

**대신 편집 방식이 원장을 지킨다.** darak은 `usersync member`를 호출하고, 그건 YAML 구문 트리를 편집한다 — 구조체로 디코딩했다 다시 인코딩하면 실제 roster는 46줄 중 17줄이 다시 쓰인다(주석·빈 줄 소실, flow 시퀀스 전개, 키 순서 변경). AST 경로에서는 **한 줄만** 바뀌므로 이력이 계속 읽을 수 있는 것으로 남는다. 쓰기 전에 로드·검증하고(그렇지 않으면 잘못된 웹 요청이 다음 부팅을 막는다 — 엔트리포인트가 `usersync validate`로 시작한다), roster 옆 사이드카에 flock을 잡는다(두 소유자가 동시에 편집하면 한쪽이 조용히 사라진다). 편집 뒤 `usersync apply`를 **동기로** 실행한다 — 그러지 않으면 요청은 성공하고 아무 일도 안 일어난 채 다음 재시작까지 간다.

**그래서 `/etc/darak`가 쓰기 가능해졌다.** 예전에는 읽기 전용이었고 그 근거는 지금도 유효하다 — 다만 그 근거가 지키는 것은 *원장*이지 파일 전체가 아니다. darak이 쓰는 것은 `users[].groups` 한 필드뿐이다.

**거절은 404다.** 로그인은 했지만 관리자가 아닌 사람에게 403을 주면 *"여기에 관리 API가 있다"* 를 확인해 주는 셈이다. 팀 라우트도 같다 — 소유하지 않은 팀에 대한 요청은 404라서, 어떤 팀이 있고 누가 소유하는지가 새지 않는다. 얻을 것이 없으므로 존재하지 않는 것처럼 답한다. 관리자 그룹이 아예 없는 시스템에서도 **아무도 관리자가 아니다** — 그룹이 없다는 것은 오류가 아니라 답이고, 여기서 열어 주면 *"아직 그룹을 안 만들었다"* 가 *"전원이 계정을 관리할 수 있다"* 가 된다.

**사용자별 사용량은 백그라운드에서 잰다.** ZFS가 있으면 `zfs userspace`가 파일 수와 무관하게 즉답한다. 없으면 트리를 한 번 순회한다. 어느 쪽이든 요청 안에서 계산하지 않는다 — 디스크가 가득 차고 느려졌을 때가 이 페이지를 여는 때인데, 바로 그때 페이지가 죽는다. 마지막 측정값을 **잰 시각과 함께** 보여주고, 실패하면 이전 값을 유지한 채 실패를 표시한다.

---

## 5. 권한 레이아웃

### 디렉터리 구조

```
/srv/data/                   0755
├── homes/                   0711  ← 통과만, 목록 불가
│   └── <username>/          0700, owner=<user>
└── teams/                   0755
    └── <teamname>/          2770, group=team-<teamname>
```

**`homes/`가 0711인 이유.** 자기 홈에 들어가는 데 필요한 것은 `x`뿐이고, `r`은 **전 직원의 계정명을 나열하는 데만** 쓰인다. 0755로 두면 누구나 `ls /srv/data/homes`로 사내 인명부를 얻는다 — 아무 폴더도 열지 못하면서. 접근 통제가 무너지는 건 아니지만 보여줄 이유가 없는 것을 보여주는 것이고, 웹에서는 그게 더 나빴다: 빵부스러기의 *"내 폴더"* 링크가 `homes/`를 가리켜서, 자기 폴더 이름을 누르면 동료 목록이 나왔다.

통과만 하는 경로는 전부 그대로 동작한다 — 헬퍼의 `openat2`도, Samba `[homes]`의 `path = …/%U`도 부모를 나열하지 않는다. 실측으로 확인했다.

`teams/`는 0755로 둔다. **팀 이름은 개인정보가 아니고**, 어떤 팀이 있는지 보이는 편이 쓸모 있으며, 폴더 자체가 2770이라 비구성원은 여전히 못 들어간다.

### 팀 디렉터리 초기화

```bash
mkdir -p /srv/data/teams/design
chgrp -R team-design /srv/data/teams/design
chmod -R g+rwX      /srv/data/teams/design
find /srv/data/teams/design -type d -exec chmod g+s {} +   # setgid는 디렉터리에만
```

**setgid 비트가 핵심이다.** 이게 없으면 하위에 생성되는 파일이 생성자의 기본 그룹을 따라가 팀원이 수정할 수 없게 된다.

> **`chmod -R g+ws`를 쓰지 말 것.** `-R`은 일반 파일에도 setgid를 붙인다(`X`와 달리 `s`에는 디렉터리 전용 예외가 없다). 의도한 것은 디렉터리의 setgid이므로 위처럼 분리한다. (`g+rwX`의 대문자 `X`는 이미 실행 비트가 있는 것과 디렉터리에만 `x`를 준다.)
>
> 일반 파일의 setgid는 **아무 효과도 없다** — 예전에 mandatory locking 마커로 쓰이긴 했으나 그 기능은 Linux 5.15에서 제거됐고, 그전에도 `-o mand` 마운트가 필요했으며 XFS 기본 마운트나 CephFS는 지원한 적이 없다. 문제는 위험해서가 아니라 **의미 없는 비트가 남아 보안 스캐너가 매번 걸고 넘어지고, 읽는 사람이 의도를 오해한다**는 것이다.

### Samba 공유 설정

> 이 블록은 [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)에 따라 `usersync shares --write`가 생성한다. 아래는 그 출력과 일치해야 하는 참조본이다.

```ini
[homes]
   path = /srv/data/homes/%U       # ← 필수. 없으면 passwd의 홈을 쓴다
   browseable = no
   read only = no
   valid users = %S
   create mask = 0600
   directory mask = 0700

[team-design]
   path = /srv/data/teams/design
   valid users = @team-design
   force group = team-design
   create mask = 0660
   directory mask = 2770
   force directory mode = 2770
```

**mode 산술을 정확히 알고 써야 한다.** Samba는 `(base & mask) | force`로 새 객체의 mode를 정하는데, **SMB에는 유닉스 mode가 흐르지 않으므로** `base`는 클라이언트 요청이 아니라 **smbd가 DOS 속성에서 유도한 값**이다 — 파일 `0666`(archive 속성이 owner-execute로 매핑되면 `0766`), 디렉터리 `0777`.

| 설정 | 파일 | 폴더 | 판단 |
|---|---|---|---|
| 없음(전역 기본 `0744`/`0755`) | `0744` | `2755` | group·other 비트가 남는다 |
| `create mask = 0660` / `directory mask = 2770` | `0660` | `2770` | **이것만으로 충분하다** |
| + `force create mode = 0660` | `0660` | — | **차이 없음.** 넣지 않는다 |
| mask가 force보다 넓음 (`0664` + `0660`) | `0664` | — | **world-readable**이 조용히 남는다 |

**`force directory mode = 2770`만은 실제로 필요하다.** mask는 비트를 **지우기만** 하므로 setgid를 되돌릴 수 없다. 부모 디렉터리가 setgid를 잃으면(드리프트) 그 아래 SMB로 만든 폴더가 `0770`·setgid 없음으로 떨어지고, **그 안에 생성되는 파일이 생성자의 기본 그룹을 따라가** 팀원이 수정하지 못한다 — 한 단계 아래에서 벌어지고 클라이언트에는 단서가 없다. force가 어느 깊이에서든 setgid를 복원한다.

> 위 숫자는 추론이 아니라 **실제 smbd로 측정한 값**이다. usersync 저장소의 `scripts/verify-samba-modes.sh`가 재현한다(부모 `2770`: mask만으로 `0660`/`2770` — force 무의미. 부모 `0770`: mask만 `0770`, force 있으면 `2770`).

**`[homes]`의 `path`도 필수다.** Samba의 `[homes]`는 `path`가 없으면 `getpwnam()`의 홈 디렉터리를 쓴다. 계정 프로비저닝이 passwd의 홈을 `/srv/data/homes/<user>`로 잡아준다면 생략해도 맞지만, 그 일치에 암묵적으로 기대는 대신 명시한다.

### ⚠️ 웹 경로도 동일한 mode로 생성해야 함

누락 시 *"웹으로 올린 파일만 팀원이 수정 못 하는"* 버그가 생기며, 사용자는 원인을 짐작조차 못 한다. 두 가지를 지킬 것:

1. **umask를 전역 고정하지 말 것.** 홈은 `0077`, 팀은 `0002`로 **대상 트리마다 다르다.** `0002`로 고정하면 홈에 웹으로 올린 파일이 `0664`가 되어 SMB 경로(`0600`)와 어긋난다 — 홈이 `0700`이라 실제 유출은 없지만 *"두 경로가 동일한 권한 규칙"*이라는 요구사항이 이미 깨진다.
2. **더 안전한 방법은 umask에 기대지 않는 것이다.** umask는 프로세스 전역 상태라 동시 요청을 처리하는 헬퍼에서 레이스가 난다. 헬퍼가 생성 직후 `fchmod`로 목표 mode를 명시적으로 박는 편이 낫다.

---

## 6. 파일 쓰기 규약

모든 쓰기는 아래 순서를 따른다. **예외 없음.**

```go
// 1. 같은 디렉터리에 임시 파일 생성 (mode/group 규칙 적용)
tmp := filepath.Join(dir, ".upload-"+random)

// 2. 내용 기록 후 fsync(파일)

// 3. 기존 파일이 있으면 휴지통에 하드링크로 고정 (target은 그대로 살아있다)
os.Link(target, trashPath)     // .trash/2026-08-08T13:22_report.xlsx

// 4. 원자적 교체 — 단 한 번
os.Rename(tmp, target)

// 5. fsync(부모 디렉터리)
```

**왜 이 순서인가**

- `rename(2)`는 원자적이다. 읽는 쪽은 항상 옛 파일 또는 새 파일만 보며, 중간 상태를 절대 보지 않는다. CephFS도 같은 디렉터리 내에서 보장한다
- 업로드가 중단되면 임시 파일만 남고 원본은 무사하다
- 3번 덕분에 LWW로 덮어쓴 작업이 **항상 복구 가능**하다. 사실상 버전 관리이며 구현 비용은 링크 한 번. 휴지통 기능이 덤으로 생긴다

**왜 `rename(target→휴지통)`이 아니라 `link`인가.** rename 두 번이면 그 사이에 **target이 존재하지 않는 창**이 열린다. 그때 읽던 클라이언트는 ENOENT를 받는다 — *"읽는 쪽은 항상 옛 파일 또는 새 파일만 본다"*는 위 주장이 실제로는 성립하지 않게 된다. `link`는 옛 inode를 휴지통에 붙들어두면서 target을 살려두므로, 교체가 **단 한 번의 원자적 rename**으로 끝나고 빈 창이 없다. `link`도 `rename`과 같이 같은 파일시스템 내여야 하므로 제약이 늘지 않는다. 다만 CephFS의 하드링크는 backtrace 갱신 비용이 있으니 성능은 측정할 것.

**주의 (정정).** `rename`은 새 inode를 만들지 않는다 — 기존 디렉터리 엔트리가 임시 파일의 inode를 가리키도록 옮길 뿐이다. 결과적으로 **소유권·mode가 임시 파일 것을 따르는 것은 맞다.** 또한 setgid 디렉터리가 상속시키는 것은 **그룹뿐이고 mode는 상속되지 않으므로**, [5장](#-웹-경로도-동일한-mode로-생성해야-함)의 mode 규칙을 임시 파일 생성 시점에 명시적으로 적용해야 한다.

**5번(디렉터리 fsync)이 빠지면 안 된다.** 파일만 fsync하면 크래시 시 디렉터리 엔트리가 유실될 수 있다.

**신규 생성 경로**에는 `renameat2(RENAME_NOREPLACE)`를 쓰면 *"덮어쓰기 / 사본 저장"* 분기가 깔끔해진다. CephFS의 `renameat2` 플래그 지원 범위는 확인이 필요하다.

### 휴지통 위치

**권한 도메인마다 그 도메인 루트에 둔다** — `teams/design/.trash/`, `homes/alice/.trash/`.

팀 파일을 개인 홈 밑 휴지통으로 보내면 **다른 팀원이 복원할 수 없고**, 데이터가 팀 권한 도메인 밖으로 나간다. 반대로 팀 디렉터리 안에 두면 SMB에서 그대로 보이므로 Samba의 `veto files` / `hide files`로 가린다. 쿼터 정산 대상에 포함할지도 함께 결정할 것([10장](#10-미결정-사항)).

**이름 충돌.** 휴지통 이름은 `<UTC 초>_<basename>`이라 1초 해상도다. 겹치면 **접미사를 붙여 다시 시도**하고, 두 판본이 모두 남는다. 마지막 되돌리기 수단이 조용히 한 판본을 먹는 것은 [ADR-6](#adr-6-동시-쓰기--last-write-wins)의 근거를 무너뜨리므로 이건 선택이 아니다.

**그래서 파일시스템에 요구사항이 하나 생긴다: `renameat2(RENAME_NOREPLACE)`.** 이것 없이는 "겹쳤는지"를 알 방법이 없다(`rename`은 조용히 대체하고, `stat`으로 먼저 확인하는 것은 TOCTOU다). 지원하지 않는 파일시스템에서는 **삭제를 거부**한다 — 평범한 `rename`으로 물러서면 그게 바로 막으려던 조용한 덮어쓰기이고, 하필 그런 배포가 알아채기 가장 어렵다. ext4·ZFS는 확인했고, **CephFS는 확인해야 한다**([§7](#지금-피해둘-것-cephfs-호환성)).

**정리 작업.** 30일 경과한 휴지통 항목 삭제 cron. 고아 임시 파일(`.upload-*`) 정리 cron.

---

## 7. 확장성과 이후 경로

### 스토리지 확장 (단일 → CephFS)

**난이도: 낮음.** 마운트 지점 교체만으로 완료. Go 서버 코드 변경 없음.

체감 차이는 **지연 시간 프로파일**에서 발생한다.

- 메타데이터 연산이 MDS로 네트워크 왕복 → `stat`이 마이크로초에서 밀리초 단위로
- Windows 탐색기는 stat-heavy(항목별 아이콘·썸네일·크기 조회). 대용량 디렉터리에서 체감
- 작은 파일 대량 쓰기는 느려짐(왕복 + 복제본 확정 대기)
- 반대로 큰 파일 순차 전송은 OSD 병렬 분산으로 **더 빠를 수 있음**

사무용 파일 공유 워크로드에서는 MDS 캐시가 잘 들어 대체로 수용 가능한 수준.

### 게이트웨이 다중화

**난이도: 높음. 여기가 진짜 프로젝트다.**

SMB의 상태 — 락, oplock/lease, share mode — 는 파일시스템이 아니라 **Samba 프로세스의 메모리와 tdb**에 있다. 노드 2대가 되면 서로의 락을 모른다. 서로 다른 게이트웨이로 접속한 두 사용자가 같은 파일을 열면 둘 다 쓰기 권한을 얻는다.

해법은 **CTDB**(clustered TDB). Ceph 위 Samba의 표준 구성이지만 recovery lock 공유 저장소, 노드 간 하트비트, public IP 페일오버가 따라붙는다.

**단, 스토리지가 커진다고 게이트웨이가 자동으로 부족해지지는 않는다.** 사내 규모에서는 1대로 상당 기간 운영 가능하다.

### 지금 피해둘 것 (CephFS 호환성)

| 하지 말 것 | 대신 |
|---|---|
| `fallocate` 사전 할당 후 offset 쓰기 | 청크를 별도 파일로 쌓았다가 마지막에 병합 |
| SQLite 등 앱 DB를 데이터 볼륨에 배치 | 로컬 또는 별도 DB. 게이트웨이 다중화 시에도 이 분리가 유효 |
| 락 파일 생성/존재 확인 방식 | `flock` / `fcntl` (CephFS가 클러스터 전역 지원) |
| inode 번호를 파일 식별자로 사용 | 자체 ID 발급 후 xattr 또는 DB로 관리 |

**전환 전에 반드시 확인할 것 — `renameat2(RENAME_NOREPLACE)`.** 삭제 경로가 여기 의존한다([§6](#휴지통-위치)). CephFS가 지원하지 않으면 **전환 시점에 삭제가 전부 실패한다.** 커널 클라이언트와 ceph-fuse가 다를 수 있으므로 실제로 쓸 마운트에서 확인한다:

```c
renameat2(AT_FDCWD, "a", AT_FDCWD, "b", RENAME_NOREPLACE)
  → EEXIST  지원          → EINVAL  미지원 (전환 불가)
```

---

## 8. 읽기 경로 최적화

동시성에서 아낀 예산을 여기 투입한다.

- **조건부 GET** — `If-None-Match` / `If-Modified-Since`
- **Range 요청** — 영상 스크럽, 다운로드 재개에 필수
- **썸네일 캐시** — 원본이 아닌 별도 저장소에
- **디렉터리 목록** — CephFS에서 항목별 `stat`은 비싸다. `getdents`로 일괄 조회 후 필요한 필드만 채우거나, 목록을 짧게 캐싱

> 단일 노드에서 실측해보니 **큰 디렉터리의 병목은 서버가 아니라 브라우저였다.** 50,000개 목록에서 서버 응답은 2.3초인데 첫 행이 그려지기까지 34초가 걸렸다 — DOM 노드 800,070개를 만드느라. 보이는 만큼만 그리도록 고치니 2.4초(= 서버 응답 시간 그 자체)가 됐고, 같은 목록의 전선 크기는 gzip으로 4.78MB → 178KB가 됐다. **위 항목들을 손대기 전에 어느 쪽이 실제로 느린지부터 재는 편이 낫다.** CephFS에서는 균형이 다시 서버 쪽으로 기울 수 있다.

---

## 9. 구현 계획

### Phase 0 — 기반

> **구현 완료** (2026-08-08). `internal/wire` · `internal/helper` · `internal/helperpool` · `internal/lint`,
> 실제 uid 분리 검증은 `internal/integration`. 강등은 `setpriv` 대신 자식 프로세스 credential 을 쓴다
> (의존성 감소 + 보조 그룹을 명시적 목록으로 넘겨 변경 감지가 가능해짐). 상세는 `docs/helper-protocol.md`.

> **이 단계가 이 프로젝트에서 가장 무거운 항목이다.** [ADR-3의 프로토콜 범위](#-헬퍼-프로토콜의-범위--이-설계-최대의-함정) 참조 — 불변식 하나만 놓쳐도 ADR-3이 배제한 보안 사고가 그대로 돌아온다.

- [x] 헬퍼 **오퍼레이션 집합** 설계 (유닉스 소켓 + `SCM_RIGHTS`) — `OPEN`/`OPENAT_CREATE`/`MKDIR`/`RENAME`/`LINK`/`UNLINK`/`RMDIR`/`STAT`/`CHMOD`/`SETXATTR`/`READDIR`
- [x] 요청 다중화 (head-of-line blocking 방지)
- [x] 헬퍼 기동 / 유휴 회수 / 그룹 변경 시 강제 재기동
- [x] `openat2(RESOLVE_BENEATH)` 래퍼 — **헬퍼 안에** 배치
- [x] 불변식 강제: 서버는 경로 기반 syscall·`*at()`을 직접 호출하지 않음 (lint 또는 리뷰 체크리스트)
- [x] mode 규칙 적용 지점 확정 (umask 아닌 명시적 `fchmod` 권장)
- [ ] usersync 배포 + `roster.yaml` 부트스트랩 ([ADR-8](#adr-8-계정-생명주기는-usersync에-위임))
- [ ] winbindd 기동 (`ntlm_auth` 전제, [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택))

### Phase 1 — 최소 동작

> **부분 완료** (2026-08-08). API·인증·쓰기 규약·휴지통은 `internal/{auth,vfs,server}` 에 있고
> 컨테이너 e2e 로 검증된다. 웹 UI 와 usersync 배포가 남았다.
>
> ⚠️ **정정**: `ntlm_auth` 의 평문 검증 프로토콜은 `ntlm-server-1` 이 아니라 **`squid-2.5-basic`** 이다.
> 전자는 NTLM 챌린지/응답이라 올바른 비밀번호에도 `Authenticated: No` 를 답한다. 또한 두 필드가
> **무조건 언이스케이프**되므로 퍼센트 인코딩이 안전장치가 아니라 정확성 요건이다(`%` 든 비밀번호가 거부됨).
> 실제 Samba 4.22 로 측정했다.

- [x] 인증 — `ntlm_auth` → tdbsam. `Authenticator` 인터페이스 뒤에 배치
- [x] 디렉터리 목록 / 다운로드 (Range 포함)
- [x] 업로드 (임시 파일 → fsync → link → rename → dir fsync)
- [ ] 휴지통 (덮어쓰기 시 자동, 수동 복원 UI)
- [ ] 디렉터리 레이아웃 및 Samba 공유 구성 (`usersync shares --write`)

### Phase 2 — 운영 편의

- [x] 웹 UI (모바일 포함) — 비숙련 사용자 기준 실사용 검증은 배포 후
- [x] 공유 링크 발급 (만료·비밀번호·취소) — [ADR-1의 좁은 예외](#-공유-링크--채택--셀프서비스-공유--비목표)
- [ ] 감사 로그 — 웹만 ([SMB는 비대칭 수용](#-데이터-보호의-경로-비대칭--수용))
- [ ] 썸네일 / 미리보기 — **비특권 seccomp 워커로 분리**
- [ ] 휴지통·고아 임시 파일 정리 cron
- [ ] 백업/스냅샷 정책 수립 — 휴지통은 백업이 아니다
- [ ] `usersync audit` 정기 실행 (계정 드리프트 감시)

### Phase 3 — 확장

- [ ] CephFS 전환 — **`RENAME_NOREPLACE` 지원 확인이 선행**([§7](#지금-피해둘-것-cephfs-호환성))
- [x] 이름 검색 — **색인 없이**, 요청자 자격으로 걷는 방식([위 근거](#-검색--색인을-두지-않는-것으로-해소))
- [ ] 검색 인덱싱 / 내용 검색 — 위 유출 문제를 함께 설계해야 함
- [ ] ETag + `If-Match` 기반 덮어쓰기 경고 (409 → "덮어쓰기 / 사본 저장" 선택)
- [ ] (필요 시) 게이트웨이 다중화 + CTDB

---

## 10. 미결정 사항

### ✅ SMB 비밀번호 — 해소됨

> **결정: 자격증명 저장소를 tdbsam 하나로 통일한다.** 웹은 `ntlm_auth`로 같은 것을 검증한다. 사용자당 비밀번호는 하나이고, 웹용/SMB용이 갈릴 여지가 없다. 근거·대가·뒤집는 조건은 [ADR-2](#adr-2-인증은-tdbsam-하나-sso-미채택).
>
> 이전에 검토했던 `smbpasswd -s` 동기화는 **웹이 SSO를 쓸 때만 필요한 우회**였으므로 함께 폐기된다. AD 도메인 참여는 여전히 옳은 최종 형태이지만 **도입 시기가 미정**이라 지금 기다릴 수 없다 → [ADR-8](#adr-8-계정-생명주기는-usersync에-위임).

### ✅ 공유 링크 — 채택 / 셀프서비스 공유 — 비목표

> **결정.** 공유 링크는 만든다. 셀프서비스 공유(사내 다른 **사용자**에게 지속적 접근 권한 부여)는 하지 않는다.

이전 개정에서 둘을 "같은 결정"으로 묶었는데 **과했다.** 둘 다 [ADR-1](#adr-1-권한-모델은-posix-uidgid)의 예외이긴 하나 크기가 다르다.

| | 셀프서비스 공유 | 공유 링크 |
|---|---|---|
| 근거 | 신원(누구인가) | **능력(URL을 가졌는가)** |
| 지속성 | 무기한 | 만료 있음 |
| 파일시스템 | POSIX 그룹/ACL 변경 필요 → **그룹 폭발** | **변경 없음** |
| 대상 | 사내 사용자 | 아무나(링크 보유자) |

**공유 링크는 파일시스템을 건드리지 않는다.** 서버가 발급자 권한으로 파일을 열어 스트리밍할 뿐이다. 그래서 그룹 폭발 문제가 발생하지 않고, ADR-1 예외의 범위가 "링크로 지정된 그 파일 하나, 만료까지"로 좁게 한정된다.

**S3 presigned URL과의 차이 — 상태를 가진다.** presigned URL은 서명 검증만 하므로 만료 전 취소가 불가능하다. 이 설계는 취소 가능성에 기대고 있으므로(세션을 서버에 두는 이유가 로그아웃 즉시 반영, 오프보딩이 양쪽을 동시에 닫는 것) 토큰을 서버가 보관하고 **명시적 취소**를 지원한다.

**자동으로 닫히는 것.** 매 접근마다 발급자의 헬퍼로 파일을 열므로 커널이 다시 검사한다 → 발급자가 팀에서 빠지거나 파일이 지워지면 링크도 죽는다. 계정이 사라져도 죽는다.

**자동으로 닫히지 않는 것.** `status: disabled`는 계정을 남기므로 그 사용자의 링크는 계속 동작한다. 그래서 **링크에 최대 수명 상한**을 둔다(기본 30일). 오프보딩 체크리스트에 "발급한 링크 취소"를 넣을 것.

**셀프서비스 공유는 명시적 비목표로 내린다.** 필요하다는 요구가 실제로 올라오면 그때 다시 연다.

### ✅ 데이터 보호의 경로 비대칭 — 수용

> **결정.** SMB 경로에는 휴지통·감사 로그를 제공하지 않는다. **SMB는 숙련 사용자용 부가 경로**이므로([2장](#2-요구사항)) 자기 데이터 보호는 사용자 책임으로 둔다.

| | 웹 | SMB |
|---|---|---|
| 삭제 시 휴지통 | ✅ | ❌ **수용** |
| 덮어쓰기 시 휴지통 | ✅ | ❌ **수용** (in-place write라 커버 불가) |
| 감사 로그 | ✅ | ❌ **수용** |

**단, [ADR-6](#adr-6-동시-쓰기--last-write-wins)의 근거를 이 결정에 맞게 읽어야 한다.** 거기서 LWW를 수용한 논거는 *"손실이 항상 복구 가능하다"* 였는데, 그 보장은 **웹 경로에만** 있다. 즉 정확히는 이렇다:

> 웹으로 덮어쓴 것은 항상 복구된다. SMB로 in-place 덮어쓴 것은 복구되지 않으며, 그건 SMB를 쓰기로 한 숙련 사용자가 감수한다.

**나중에 마음이 바뀌면 싼 것 두 개.** `vfs objects = recycle`(삭제만 커버)과 `vfs objects = full_audit`은 각각 한 줄이다. 덮어쓰기는 어느 쪽으로도 커버되지 않으므로 그때도 완전 대칭은 안 된다.

### ✅ 검색 — 색인을 두지 않는 것으로 해소

검색 **인덱싱**은 [ADR-1](#adr-1-권한-모델은-posix-uidgid)과 정면으로 부딪힌다. 인덱스는 태생적으로 전역이고 권한의 진실은 파일시스템에만 있다. 결과 목록을 만들 때 **항목마다 해당 사용자 헬퍼로 접근 가능 여부를 재확인**하지 않으면 파일명·경로가 유출된다(파일명 자체가 기밀인 경우는 흔하다). 이 재확인은 비싸고, *"상위 20건 뽑고 필터링하니 3건 남음"* 같은 페이지네이션 문제도 따라온다.

**그래서 색인을 두지 않았다.** 검색은 요청마다 **그 사용자로 트리를 실제로 걷는다** — 이미 있는 READDIR을 디렉터리 하나씩, 헬퍼를 통해. 권한 판정은 평소와 똑같이 커널이 하고, 못 읽는 디렉터리는 그냥 건너뛴다. 필터링해야 할 전역 목록이 애초에 생기지 않으므로 위 문제가 발생하지 않는다.

대가는 **범위와 시간을 한도로 묶어야 한다**는 것이다: 깊이 8, 방문 20,000개, 결과 1,000건, 2초. 한도에 걸리면 응답이 **걸렸다고 말한다** — 잘린 목록은 "여기 없다"와 구별되지 않고 그 둘은 정반대 답이다. 대신 색인의 숙명인 **낡음이 없다** — 방금 올린 파일이 바로 찾힌다.

> 되돌아오는 조건: 트리가 20,000개 한도로 감당이 안 될 만큼 커지거나, **내용** 검색이 필요해질 때. 그때는 위 문단의 유출 문제가 그대로 돌아오므로, 색인을 도입하는 결정은 그 재확인 비용을 함께 설계해야 한다.

### 🟡 기타

- **백업/DR 전략이 없다.** 휴지통은 백업이 아니다(랜섬웨어가 휴지통도 지운다). 초기 단일 노드 구간이 오히려 가장 위험하다 — 스냅샷 정책을 [Phase 2](#phase-2--운영-편의)에 넣었다
- 쿼터 정책 — CephFS는 유저 쿼터가 없고 **디렉터리 xattr 기반**이다. "사용자당 100GB"를 원하면 홈 디렉터리에 거는 방식이며, **디렉터리 레이아웃 확정 시점에 함께 결정**해야 한다. 휴지통을 쿼터에 포함할지도 같이 정할 것
- CephFS POSIX ACL은 커널 클라이언트 + `acl` 마운트 옵션에서만 정상 동작(ceph-fuse는 지원 약함)
- 검색 인덱싱 방식 — 별도 인덱서 vs 실시간
- 업로드 재개/청크 — [7장](#지금-피해둘-것-cephfs-호환성)은 "청크를 별도 파일로 쌓았다가 병합"이라 했는데 [6장](#6-파일-쓰기-규약)은 "임시 파일 하나, 예외 없음"이다. **서로 충돌한다** — 청크 병합 경로에도 최종 rename이 적용되도록 규약을 다시 쓸 것

---

## 부록: 참고 프로젝트

- **Filebrowser** (Go) — 웹 파일 매니저. 구조 참고용
- **Filestash** (Go) — 웹 서버가 localhost sshd에 해당 사용자로 SFTP 접속해 임퍼소네이션을 위임. 본 프로젝트에서는 SMB를 직접 제공하므로 SSH 경유는 불필요하나, "검증된 데몬에 임퍼소네이션을 떠넘긴다"는 발상은 ADR-3의 배경
- **Samba** — 헬퍼 프로세스 + fd 패싱 구조의 원형
- **usersync** (Go, 자체 개발) — 계정 생명주기 reconciler. [ADR-8](#adr-8-계정-생명주기는-usersync에-위임)에서 채택. uid 재사용 방지(tombstone), Samba 공유 블록 생성, AD 인계 경로(`export --format csv|ldif` / `detach` / `mode: audit`)를 제공한다. 자격증명·AD 전환 계획은 그 저장소의 `identity-roadmap.md`
